global ptr value init with i32 0
i32 main() {
entry:  pred()
  value.load14 = load i32 from ptr @value
  715 = icmp slt i32 0, i32 %value.load14
  br i1 %715, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry)
  br bb %while.body
while.body:  pred(while.body, while.body_preheader)
  a.phi17 = phi [bb %while.body, i32 %11, bb %while.body_preheader, i32 0]
  11 = add i32 %a.phi17, i32 1
  7 = icmp slt i32 %11, i32 %value.load14
  br i1 %7, bb %while.body, bb %while.body_exclu_exit
while.exit:  pred(entry, while.body_exclu_exit)
  16 = phi [bb %while.body_exclu_exit, i32 %11, bb %entry, i32 0]
  ret i32 %16
while.body_exclu_exit:  pred(while.body)
  br bb %while.exit
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


