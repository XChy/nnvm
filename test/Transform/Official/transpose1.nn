global ptr a init with 100000 x i32 zeros
global ptr matrix init with 20000000 x i32 zeros
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry67:
  69 = call i32 getint()
  71 = call i32 getarray(ptr @a)
  call void _sysy_starttime(i32 28)
  br bb %while.cond74
while.cond74:
  i73.phi134 = phi [bb %entry67, i32 0, bb %while.body75, i32 %86]
  79 = icmp slt i32 %i73.phi134, i32 %69
  br i1 %79, bb %while.body75, bb %while.exit76
while.body75:
  82 = mul i32 %i73.phi134, i32 4
  83 = ptradd ptr @matrix, i32 %82
  store i32 %i73.phi134 to ptr %83
  86 = add i32 %i73.phi134, i32 1
  br bb %while.cond74
while.exit76:
  br bb %while.cond87
while.cond87:
  i73.phi133 = phi [bb %while.exit76, i32 0, bb %transpose.while.exit, i32 %101]
  92 = icmp slt i32 %i73.phi133, i32 %71
  br i1 %92, bb %while.body88, bb %while.exit89
while.body88:
  96 = mul i32 %i73.phi133, i32 4
  97 = ptradd ptr @a, i32 %96
  97.load = load i32 from ptr %97
  6144 = sdiv i32 %69, i32 %97.load
  br bb %transpose.while.cond
while.exit89:
  br bb %while.cond103
while.cond103:
  ans.phi136 = phi [bb %while.exit89, i32 0, bb %while.body104, i32 %119]
  i73.phi = phi [bb %while.exit89, i32 0, bb %while.body104, i32 %121]
  108 = icmp slt i32 %i73.phi, i32 %71
  br i1 %108, bb %while.body104, bb %while.exit105
while.body104:
  113 = mul i32 %i73.phi, i32 %i73.phi
  115 = mul i32 %i73.phi, i32 4
  116 = ptradd ptr @matrix, i32 %115
  116.load = load i32 from ptr %116
  118 = mul i32 %113, i32 %116.load
  119 = add i32 %ans.phi136, i32 %118
  121 = add i32 %i73.phi, i32 1
  br bb %while.cond103
while.exit105:
  123 = icmp slt i32 %ans.phi136, i32 0
  br i1 %123, bb %then124, bb %if.exit125
then124:
  127 = sub i32 0, i32 %ans.phi136
  br bb %if.exit125
if.exit125:
  ans.phi = phi [bb %while.exit105, i32 %ans.phi136, bb %then124, i32 %127]
  call void _sysy_stoptime(i32 47)
  call void putint(i32 %ans.phi)
  call void putch(i32 10)
  ret i32 0
transpose.while.cond:
  curr.phi141147 = phi [bb %while.body88, i32 ub, bb %transpose.while.exit18, i32 %curr.phi156]
  i.phi148 = phi [bb %while.body88, i32 0, bb %transpose.while.exit18, i32 %65164]
  14149 = icmp slt i32 %i.phi148, i32 %6144
  br i1 %14149, bb %transpose.while.body, bb %transpose.while.exit
transpose.while.body:
  br bb %transpose.while.cond16
transpose.while.exit:
  101 = add i32 %i73.phi133, i32 1
  br bb %while.cond87
transpose.while.cond16:
  curr.phi156 = phi [bb %transpose.while.body, i32 %curr.phi141147, bb %transpose.if.exit, i32 %37.load174, bb %transpose.then, i32 %curr.phi156]
  j.phi157 = phi [bb %transpose.while.body, i32 0, bb %transpose.if.exit, i32 %63182, bb %transpose.then, i32 %29167]
  21158 = icmp slt i32 %j.phi157, i32 %97.load
  br i1 %21158, bb %transpose.while.body17, bb %transpose.while.exit18
transpose.while.body17:
  25161 = icmp slt i32 %i.phi148, i32 %j.phi157
  br i1 %25161, bb %transpose.then, bb %transpose.if.exit
transpose.while.exit18:
  65164 = add i32 %i.phi148, i32 1
  br bb %transpose.while.cond
transpose.then:
  29167 = add i32 %j.phi157, i32 1
  br bb %transpose.while.cond16
transpose.if.exit:
  33170 = mul i32 %i.phi148, i32 %97.load
  35171 = add i32 %33170, i32 %j.phi157
  36172 = mul i32 %35171, i32 4
  37173 = ptradd ptr @matrix, i32 %36172
  37.load174 = load i32 from ptr %37173
  41175 = mul i32 %j.phi157, i32 %6144
  43176 = add i32 %41175, i32 %i.phi148
  44177 = mul i32 %43176, i32 4
  45178 = ptradd ptr @matrix, i32 %44177
  52.load179 = load i32 from ptr %37173
  store i32 %52.load179 to ptr %45178
  store i32 %37.load174 to ptr %37173
  63182 = add i32 %j.phi157, i32 1
  br bb %transpose.while.cond16
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

i32 transpose(i32 n, ptr matrix, i32 rowsize) {
entry:
  6 = sdiv i32 %n, i32 %rowsize
  br bb %while.cond
while.cond:
  curr.phi141 = phi [bb %entry, i32 ub, bb %while.exit18, i32 %curr.phi]
  i.phi = phi [bb %entry, i32 0, bb %while.exit18, i32 %65]
  14 = icmp slt i32 %i.phi, i32 %6
  br i1 %14, bb %while.body, bb %while.exit
while.body:
  br bb %while.cond16
while.exit:
  ret i32 -1
while.cond16:
  curr.phi = phi [bb %while.body, i32 %curr.phi141, bb %if.exit, i32 %37.load, bb %then, i32 %curr.phi]
  j.phi = phi [bb %while.body, i32 0, bb %if.exit, i32 %63, bb %then, i32 %29]
  21 = icmp slt i32 %j.phi, i32 %rowsize
  br i1 %21, bb %while.body17, bb %while.exit18
while.body17:
  25 = icmp slt i32 %i.phi, i32 %j.phi
  br i1 %25, bb %then, bb %if.exit
while.exit18:
  65 = add i32 %i.phi, i32 1
  br bb %while.cond
then:
  29 = add i32 %j.phi, i32 1
  br bb %while.cond16
if.exit:
  33 = mul i32 %i.phi, i32 %rowsize
  35 = add i32 %33, i32 %j.phi
  36 = mul i32 %35, i32 4
  37 = ptradd ptr %matrix, i32 %36
  37.load = load i32 from ptr %37
  41 = mul i32 %j.phi, i32 %6
  43 = add i32 %41, i32 %i.phi
  44 = mul i32 %43, i32 4
  45 = ptradd ptr %matrix, i32 %44
  52.load = load i32 from ptr %37
  store i32 %52.load to ptr %45
  store i32 %37.load to ptr %37
  63 = add i32 %j.phi, i32 1
  br bb %while.cond16
}

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


