immutable global ptr n init with i32 10
global ptr k init with i32 0
i32 main() {
entry:  pred()
  store i32 1 to ptr @k
  926 = icmp sle i32 0, i32 9
  br i1 %926, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry)
  br bb %while.body
while.body:  pred(while.body, while.body_preheader)
  i.phi27 = phi [bb %while.body, i32 %13, bb %while.body_preheader, i32 0]
  13 = add i32 %i.phi27, i32 1
  k.load17 = load i32 from ptr @k
  25 = shl i32 %k.load17, i32 1
  store i32 %25 to ptr @k
  9 = icmp sle i32 %13, i32 9
  br i1 %9, bb %while.body, bb %while.exit
while.exit:  pred(while.body, entry)
  k.load20 = load i32 from ptr @k
  call void putint(i32 %k.load20)
  k.load22 = load i32 from ptr @k
  ret i32 %k.load22
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


