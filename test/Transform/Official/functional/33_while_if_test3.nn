external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry34:
  %p = stack 4
  store i32 2 to ptr %p
  38 = call i32 deepWhileBr(i32 2, i32 2)
  ret i32 %38
}

external float getfloat()

external void putfloat(float )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )

external void putch(i32 )

i32 deepWhileBr(i32 a, i32 b) {
entry:
  %e = stack 4
  %d = stack 4
  %c = stack 4
  %b.stack = stack 4
  %a.stack = stack 4
  store i32 %a to ptr %a.stack
  store i32 %b to ptr %b.stack
  a.stack.load = load i32 from ptr %a.stack
  6 = add i32 %a.stack.load, i32 %b
  store i32 %6 to ptr %c
  br bb %while.cond
while.cond:
  c.load = load i32 from ptr %c
  11 = icmp slt i32 %c.load, i32 75
  12 = icmp ne i1 %11, i1 0
  br i1 %12, bb %while.body, bb %while.exit
while.body:
  store i32 42 to ptr %d
  c.load14 = load i32 from ptr %c
  15 = icmp slt i32 %c.load14, i32 100
  br i1 %15, bb %then, bb %if.exit
while.exit:
  c.load33 = load i32 from ptr %c
  ret i32 %c.load33
then:
  c.load18 = load i32 from ptr %c
  d.load = load i32 from ptr %d
  20 = add i32 %c.load18, i32 %d.load
  store i32 %20 to ptr %c
  22 = icmp sgt i32 %20, i32 99
  br i1 %22, bb %then23, bb %if.exit24
if.exit:
  br bb %while.cond
then23:
  d.load26 = load i32 from ptr %d
  27 = mul i32 %d.load26, i32 2
  store i32 %27 to ptr %e
  28 = icmp eq i32 1, i32 1
  br i1 %28, bb %then29, bb %if.exit30
if.exit24:
  br bb %if.exit
then29:
  e.load = load i32 from ptr %e
  32 = mul i32 %e.load, i32 2
  store i32 %32 to ptr %c
  br bb %if.exit30
if.exit30:
  br bb %if.exit24
}


