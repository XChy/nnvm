external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

pure i32 main() {
entry35:  pred()
  br bb %ifWhile.while.body23
ifWhile.if.exit:  pred(ifWhile.while.exit, ifWhile.while.exit24)
  b.phi4047 = phi [bb %ifWhile.while.exit24, i32 %72, bb %ifWhile.while.exit, i32 %2157]
  ret i32 %b.phi4047
ifWhile.while.cond:  pred(ifWhile.while.body)
  b.phi4150 = phi [bb %ifWhile.while.body, i32 %1854]
  1451 = icmp eq i32 %b.phi4150, i32 2
  br i1 %1451, bb %ifWhile.while.body, bb %ifWhile.while.exit
ifWhile.while.body:  pred(ifWhile.while.cond)
  1854 = add i32 %b.phi4150, i32 2
  br bb %ifWhile.while.cond
ifWhile.while.exit:  pred(ifWhile.while.cond)
  2157 = add i32 %b.phi4150, i32 25
  br bb %ifWhile.if.exit
ifWhile.while.body23:  pred(ifWhile.while.body23, entry35)
  a.phi6174 = phi [bb %ifWhile.while.body23, i32 %3366, bb %entry35, i32 0]
  b.phi6073 = phi [bb %ifWhile.while.body23, i32 %4265, bb %entry35, i32 3]
  4265 = shl i32 %b.phi6073, i32 1
  3366 = add i32 %a.phi6174, i32 1
  2662 = icmp slt i32 %3366, i32 5
  br i1 %2662, bb %ifWhile.while.body23, bb %ifWhile.while.exit24
ifWhile.while.exit24:  pred(ifWhile.while.body23)
  72 = phi [bb %ifWhile.while.body23, i32 %4265]
  br bb %ifWhile.if.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

pure internal i32 ifWhile() {
entry:  pred()
  br bb %while.body23
if.exit:  pred(while.exit, while.exit24)
  b.phi40 = phi [bb %while.exit24, i32 %76, bb %while.exit, i32 %21]
  ret i32 %b.phi40
while.cond:  pred(while.body)
  b.phi41 = phi [bb %while.body, i32 %18]
  14 = icmp eq i32 %b.phi41, i32 2
  br i1 %14, bb %while.body, bb %while.exit
while.body:  pred(while.cond)
  18 = add i32 %b.phi41, i32 2
  br bb %while.cond
while.exit:  pred(while.cond)
  21 = add i32 %b.phi41, i32 25
  br bb %if.exit
while.body23:  pred(while.body23, entry)
  a.phi78 = phi [bb %while.body23, i32 %33, bb %entry, i32 0]
  b.phi77 = phi [bb %while.body23, i32 %42, bb %entry, i32 3]
  42 = shl i32 %b.phi77, i32 1
  33 = add i32 %a.phi78, i32 1
  26 = icmp slt i32 %33, i32 5
  br i1 %26, bb %while.body23, bb %while.exit24
while.exit24:  pred(while.body23)
  76 = phi [bb %while.body23, i32 %42]
  br bb %if.exit
}

external void putfarray(i32 , ptr )


