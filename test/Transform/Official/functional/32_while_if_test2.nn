external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry29:
  30 = call i32 ifWhile()
  ret i32 %30
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

i32 ifWhile() {
entry:
  4 = icmp eq i32 0, i32 5
  br i1 %4, bb %then, bb %else
then:
  br bb %while.cond
if.exit:
  34 = phi [bb %while.exit20, i32 %33, bb %while.exit, i32 %17]
  32 = phi [bb %while.exit20, i32 %31, bb %while.exit, i32 0]
  ret i32 %34
else:
  br bb %while.cond18
while.cond:
  35 = phi [bb %then, i32 3, bb %while.body, i32 %15]
  12 = icmp eq i32 %35, i32 2
  13 = icmp ne i1 %12, i1 0
  br i1 %13, bb %while.body, bb %while.exit
while.body:
  15 = add i32 %35, i32 2
  br bb %while.cond
while.exit:
  17 = add i32 %35, i32 25
  br bb %if.exit
while.cond18:
  33 = phi [bb %else, i32 3, bb %while.body19, i32 %25]
  31 = phi [bb %else, i32 0, bb %while.body19, i32 %27]
  22 = icmp slt i32 %31, i32 5
  23 = icmp ne i1 %22, i1 0
  br i1 %23, bb %while.body19, bb %while.exit20
while.body19:
  25 = mul i32 %33, i32 2
  27 = add i32 %31, i32 1
  br bb %while.cond18
while.exit20:
  br bb %if.exit
}

external void putfarray(i32 , ptr )


