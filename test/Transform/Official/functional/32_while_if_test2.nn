external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry29:
  br bb %ifWhile.while.cond18
ifWhile.if.exit:
  b.phi3440 = phi [bb %ifWhile.while.exit20, i32 %b.phi53, bb %ifWhile.while.exit, i32 %1750]
  ret i32 %b.phi3440
ifWhile.while.cond:
  b.phi3543 = phi [bb %ifWhile.while.body, i32 %1547]
  1244 = icmp eq i32 %b.phi3543, i32 2
  br i1 %1244, bb %ifWhile.while.body, bb %ifWhile.while.exit
ifWhile.while.body:
  1547 = add i32 %b.phi3543, i32 2
  br bb %ifWhile.while.cond
ifWhile.while.exit:
  1750 = add i32 %b.phi3543, i32 25
  br bb %ifWhile.if.exit
ifWhile.while.cond18:
  b.phi53 = phi [bb %entry29, i32 3, bb %ifWhile.while.body19, i32 %2558]
  a.phi54 = phi [bb %entry29, i32 0, bb %ifWhile.while.body19, i32 %2759]
  2255 = icmp slt i32 %a.phi54, i32 5
  br i1 %2255, bb %ifWhile.while.body19, bb %ifWhile.while.exit20
ifWhile.while.body19:
  2558 = mul i32 %b.phi53, i32 2
  2759 = add i32 %a.phi54, i32 1
  br bb %ifWhile.while.cond18
ifWhile.while.exit20:
  br bb %ifWhile.if.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

pure i32 ifWhile() {
entry:
  br bb %while.cond18
if.exit:
  b.phi34 = phi [bb %while.exit20, i32 %b.phi, bb %while.exit, i32 %17]
  ret i32 %b.phi34
while.cond:
  b.phi35 = phi [bb %while.body, i32 %15]
  12 = icmp eq i32 %b.phi35, i32 2
  br i1 %12, bb %while.body, bb %while.exit
while.body:
  15 = add i32 %b.phi35, i32 2
  br bb %while.cond
while.exit:
  17 = add i32 %b.phi35, i32 25
  br bb %if.exit
while.cond18:
  b.phi = phi [bb %entry, i32 3, bb %while.body19, i32 %25]
  a.phi = phi [bb %entry, i32 0, bb %while.body19, i32 %27]
  22 = icmp slt i32 %a.phi, i32 5
  br i1 %22, bb %while.body19, bb %while.exit20
while.body19:
  25 = mul i32 %b.phi, i32 2
  27 = add i32 %a.phi, i32 1
  br bb %while.cond18
while.exit20:
  br bb %if.exit
}

external void putfarray(i32 , ptr )


