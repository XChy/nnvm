i32 main() {
entry:
  %sum = stack 4
  %i = stack 4
  store i32 0 to ptr %i
  store i32 0 to ptr %sum
  br bb %while.cond
while.cond:
  i.load = load i32 from ptr %i
  7 = icmp slt i32 %i.load, i32 100
  8 = icmp ne i1 %7, i1 0
  br i1 %8, bb %while.body, bb %while.exit
while.body:
  i.load9 = load i32 from ptr %i
  10 = icmp eq i32 %i.load9, i32 50
  br i1 %10, bb %then, bb %if.exit
while.exit:
  sum.load18 = load i32 from ptr %sum
  ret i32 %sum.load18
then:
  br bb %while.exit
if.exit:
  sum.load = load i32 from ptr %sum
  i.load14 = load i32 from ptr %i
  15 = add i32 %sum.load, i32 %i.load14
  store i32 %15 to ptr %sum
  i.load16 = load i32 from ptr %i
  17 = add i32 %i.load16, i32 1
  store i32 %17 to ptr %i
  br bb %while.cond
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


