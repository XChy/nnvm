pure i32 main() {
entry:  pred()
  721 = icmp slt i32 0, i32 100
  br i1 %721, bb %while.body_preheader, bb %while.exit
while.cond:  pred(if.exit)
  sum.phi = phi [bb %if.exit, i32 %15]
  i.phi = phi [bb %if.exit, i32 %17]
  7 = icmp slt i32 %i.phi, i32 100
  br i1 %7, bb %while.body, bb %while.exit
while.body_preheader:  pred(entry)
  sum.phi23_ph_val = phi [bb %entry, i32 0]
  i.phi24_ph_val = phi [bb %entry, i32 0]
  br bb %while.body
while.body:  pred(while.cond, while.body_preheader)
  i.phi24 = phi [bb %while.cond, i32 %i.phi, bb %while.body_preheader, i32 %i.phi24_ph_val]
  sum.phi23 = phi [bb %while.cond, i32 %sum.phi, bb %while.body_preheader, i32 %sum.phi23_ph_val]
  10 = icmp eq i32 %i.phi24, i32 50
  br i1 %10, bb %while.exit, bb %if.exit
while.exit:  pred(while.cond, while.body, entry)
  22 = phi [bb %while.cond, i32 %sum.phi, bb %while.body, i32 %sum.phi, bb %entry, i32 0]
  ret i32 %22
if.exit:  pred(while.body)
  15 = add i32 %sum.phi23, i32 %i.phi24
  17 = add i32 %i.phi24, i32 1
  br bb %while.cond
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


