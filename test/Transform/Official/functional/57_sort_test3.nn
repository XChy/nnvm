global ptr n init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

internal i32 QuickSort(ptr arr, i32 low, i32 high) {
entry:  pred()
  5 = icmp slt i32 %low, i32 %high
  br i1 %5, bb %then, bb %if.exit
then:  pred(entry)
  226 = shl i32 %low, i32 2
  18 = ptradd ptr %arr, i32 %226
  18.load = load i32 from ptr %18
  64237 = add i32 %low, i32 1
  br i1 %5, bb %while.body_preheader, bb %while.exit
if.exit:  pred(entry, while.exit)
  ret i32 0
while.body_preheader:  pred(then)
  br bb %while.body
while.body:  pred(if.exit90, while.body_preheader)
  64237244 = phi [bb %if.exit90, i32 %64, bb %while.body_preheader, i32 %64237]
  229236243 = phi [bb %if.exit90, i32 %231, bb %while.body_preheader, i32 %226]
  j.phi185242 = phi [bb %if.exit90, i32 %j.phi, bb %while.body_preheader, i32 %high]
  i.phi194241 = phi [bb %if.exit90, i32 %i.phi, bb %while.body_preheader, i32 %low]
  br bb %while.cond27
while.exit:  pred(if.exit90, then)
  64237240 = phi [bb %if.exit90, i32 %64, bb %then, i32 %64237]
  229236239 = phi [bb %if.exit90, i32 %231, bb %then, i32 %226]
  low = phi [bb %if.exit90, i32 %i.phi, bb %then, i32 %low]
  104 = ptradd ptr %arr, i32 %229236239
  store i32 %18.load to ptr %104
  206 = add i32 %low, i32 -1
  114 = call i32 QuickSort(ptr %arr, i32 %low, i32 %206)
  121 = call i32 QuickSort(ptr %arr, i32 %64237240, i32 %high)
  br bb %if.exit
while.cond27:  pred(while.body, while.body28)
  j.phi187 = phi [bb %while.body, i32 %j.phi185242, bb %while.body28, i32 %207]
  32 = icmp slt i32 %i.phi194241, i32 %j.phi187
  228 = shl i32 %j.phi187, i32 2
  br i1 %32, bb %select.then, bb %select.else
while.body28:  pred(select.exit)
  br bb %while.cond27
while.exit29:  pred(select.exit)
  br i1 %32, bb %then52, bb %if.exit53
select.then:  pred(while.cond27)
  39 = ptradd ptr %arr, i32 %228
  39.load = load i32 from ptr %39
  209 = add i32 %18.load, i32 -1
  43 = icmp sgt i32 %39.load, i32 %209
  br bb %select.exit
select.else:  pred(while.cond27)
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 0, bb %select.then, i1 %43]
  207 = add i32 %j.phi187, i32 -1
  br i1 %select.phi, bb %while.body28, bb %while.exit29
then52:  pred(while.exit29)
  56 = ptradd ptr %arr, i32 %229236243
  60 = ptradd ptr %arr, i32 %228
  60.load = load i32 from ptr %60
  store i32 %60.load to ptr %56
  br bb %if.exit53
if.exit53:  pred(while.exit29, then52)
  i.phi196 = phi [bb %while.exit29, i32 %i.phi194241, bb %then52, i32 %64237244]
  br bb %while.cond65
while.cond65:  pred(if.exit53, while.body66)
  i.phi = phi [bb %if.exit53, i32 %i.phi196, bb %while.body66, i32 %85]
  70 = icmp slt i32 %i.phi, i32 %j.phi187
  231 = shl i32 %i.phi, i32 2
  br i1 %70, bb %select.then71, bb %select.else72
while.body66:  pred(select.exit73)
  85 = add i32 %i.phi, i32 1
  br bb %while.cond65
while.exit67:  pred(select.exit73)
  br i1 %70, bb %then89, bb %if.exit90
select.then71:  pred(while.cond65)
  77 = ptradd ptr %arr, i32 %231
  77.load = load i32 from ptr %77
  80 = icmp slt i32 %77.load, i32 %18.load
  br bb %select.exit73
select.else72:  pred(while.cond65)
  br bb %select.exit73
select.exit73:  pred(select.then71, select.else72)
  select74.phi = phi [bb %select.else72, i1 0, bb %select.then71, i1 %80]
  br i1 %select74.phi, bb %while.body66, bb %while.exit67
then89:  pred(while.exit67)
  93 = ptradd ptr %arr, i32 %228
  97 = ptradd ptr %arr, i32 %231
  97.load = load i32 from ptr %97
  store i32 %97.load to ptr %93
  br bb %if.exit90
if.exit90:  pred(while.exit67, then89)
  j.phi = phi [bb %while.exit67, i32 %j.phi187, bb %then89, i32 %207]
  25 = icmp slt i32 %i.phi, i32 %j.phi
  64 = add i32 %i.phi, i32 1
  br i1 %25, bb %while.body, bb %while.exit
}

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry122:  pred()
  %a = stack 40
  store i32 10 to ptr @n
  store i32 4 to ptr %a
  129 = ptradd ptr %a, i32 4
  store i32 3 to ptr %129
  132 = ptradd ptr %a, i32 8
  store i32 9 to ptr %132
  135 = ptradd ptr %a, i32 12
  store i32 2 to ptr %135
  138 = ptradd ptr %a, i32 16
  store i32 0 to ptr %138
  141 = ptradd ptr %a, i32 20
  store i32 1 to ptr %141
  144 = ptradd ptr %a, i32 24
  store i32 6 to ptr %144
  147 = ptradd ptr %a, i32 28
  store i32 5 to ptr %147
  150 = ptradd ptr %a, i32 32
  store i32 7 to ptr %150
  153 = ptradd ptr %a, i32 36
  store i32 8 to ptr %153
  162 = call i32 QuickSort(ptr %a, i32 0, i32 9)
  n.load167245 = load i32 from ptr @n
  168246 = icmp slt i32 %162, i32 %n.load167245
  br i1 %168246, bb %while.body164_preheader, bb %while.exit165
while.body164_preheader:  pred(entry122)
  br bb %while.body164
while.body164:  pred(while.body164, while.body164_preheader)
  i155.phi247 = phi [bb %while.body164, i32 %183, bb %while.body164_preheader, i32 %162]
  234 = shl i32 %i155.phi247, i32 2
  174 = ptradd ptr %a, i32 %234
  174.load = load i32 from ptr %174
  call void putint(i32 %174.load)
  call void putch(i32 10)
  183 = add i32 %i155.phi247, i32 1
  n.load167 = load i32 from ptr @n
  168 = icmp slt i32 %183, i32 %n.load167
  br i1 %168, bb %while.body164, bb %while.exit165
while.exit165:  pred(while.body164, entry122)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


