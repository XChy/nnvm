external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry37:
  41 = call i32 deepWhileBr(i32 2, i32 2)
  call void putint(i32 %41)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

i32 deepWhileBr(i32 a, i32 b) {
entry2:
  8 = add i32 %a, i32 %b
  br bb %while.cond
while.cond:
  e.phi51 = phi [bb %entry2, i32 ub, bb %if.exit, i32 %e.phi50]
  c.phi47 = phi [bb %entry2, i32 %8, bb %if.exit, i32 %c.phi46]
  13 = icmp slt i32 %c.phi47, i32 75
  14 = icmp ne i1 %13, i1 0
  br i1 %14, bb %while.body, bb %while.exit
while.body:
  17 = icmp slt i32 %c.phi47, i32 100
  br i1 %17, bb %then, bb %if.exit
while.exit:
  ret i32 %c.phi47
then:
  22 = add i32 %c.phi47, i32 42
  24 = icmp sgt i32 %22, i32 99
  br i1 %24, bb %then25, bb %if.exit26
if.exit:
  e.phi50 = phi [bb %while.body, i32 %e.phi51, bb %if.exit26, i32 %e.phi]
  c.phi46 = phi [bb %while.body, i32 %c.phi47, bb %if.exit26, i32 %c.phi45]
  br bb %while.cond
then25:
  29 = mul i32 42, i32 2
  30 = call i32 get_one(i32 0)
  31 = icmp eq i32 %30, i32 1
  br i1 %31, bb %then32, bb %if.exit33
if.exit26:
  e.phi = phi [bb %then, i32 %e.phi51, bb %if.exit33, i32 %29]
  c.phi45 = phi [bb %then, i32 %22, bb %if.exit33, i32 %c.phi]
  br bb %if.exit
then32:
  35 = mul i32 %29, i32 2
  br bb %if.exit33
if.exit33:
  c.phi = phi [bb %then25, i32 %22, bb %then32, i32 %35]
  br bb %if.exit26
}

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

i32 get_one(i32 a) {
entry:
  ret i32 1
}

external void putfarray(i32 , ptr )


