global ptr N init with i32 0
global ptr L init with i32 0
global ptr M init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

i32 main() {
entry57:  pred()
  %c2 = stack 12
  %c1 = stack 12
  %c0 = stack 24
  N.pinned = pin ptr @N
  store i32 3 to ptr %N.pinned
  store i32 3 to ptr @M
  store i32 3 to ptr @L
  84 = si2f i32 0 to float
  84271 = si2f i32 1 to float
  84290 = si2f i32 2 to float
  4204 = ptradd ptr %c1, i32 8
  store float %84271 to ptr %4204
  10208 = ptradd ptr %c2, i32 4
  store float %84290 to ptr %10208
  16212 = ptradd ptr %c0, i32 4
  store float %84 to ptr %16212
  22215 = ptradd ptr %c0, i32 8
  store float %84 to ptr %22215
  store float %84271 to ptr %c1
  store float %84290 to ptr %c2
  40224 = ptradd ptr %c1, i32 4
  store float %84271 to ptr %40224
  46228 = ptradd ptr %c2, i32 8
  store float %84290 to ptr %46228
  store float %84 to ptr %c0
  br i1 1, bb %while.body122_preheader, bb %while.exit123
while.body122_preheader:  pred(entry57)
  br bb %while.body122
while.body122:  pred(while.body122, while.body122_preheader)
  i70.phi188252 = phi [bb %while.body122, i32 %138, bb %while.body122_preheader, i32 0]
  241 = shl i32 %i70.phi188252, i32 2
  131 = ptradd ptr %c0, i32 %241
  131.load = load float from ptr %131
  133 = f2si float %131.load to i32
  call void putint(i32 %133)
  138 = add i32 %i70.phi188252, i32 1
  N.load125 = load i32 from ptr %N.pinned
  126 = icmp slt i32 %138, i32 %N.load125
  br i1 %126, bb %while.body122, bb %while.exit123
while.exit123:  pred(entry57, while.body122)
  call void putch(i32 10)
  N.load147247 = load i32 from ptr %N.pinned
  148248 = icmp slt i32 0, i32 %N.load147247
  br i1 %148248, bb %while.body144_preheader, bb %while.exit145
while.body144_preheader:  pred(while.exit123)
  br bb %while.body144
while.body144:  pred(while.body144, while.body144_preheader)
  i70.phi187249 = phi [bb %while.body144, i32 %160, bb %while.body144_preheader, i32 0]
  242 = shl i32 %i70.phi187249, i32 2
  153 = ptradd ptr %c1, i32 %242
  153.load = load float from ptr %153
  155 = f2si float %153.load to i32
  call void putint(i32 %155)
  160 = add i32 %i70.phi187249, i32 1
  N.load147 = load i32 from ptr %N.pinned
  148 = icmp slt i32 %160, i32 %N.load147
  br i1 %148, bb %while.body144, bb %while.exit145
while.exit145:  pred(while.exit123, while.body144)
  call void putch(i32 10)
  N.load169244 = load i32 from ptr %N.pinned
  170245 = icmp slt i32 0, i32 %N.load169244
  br i1 %170245, bb %while.body166_preheader, bb %while.exit167
while.body166_preheader:  pred(while.exit145)
  br bb %while.body166
while.body166:  pred(while.body166, while.body166_preheader)
  i70.phi246 = phi [bb %while.body166, i32 %182, bb %while.body166_preheader, i32 0]
  243 = shl i32 %i70.phi246, i32 2
  175 = ptradd ptr %c2, i32 %243
  175.load = load float from ptr %175
  177 = f2si float %175.load to i32
  call void putint(i32 %177)
  182 = add i32 %i70.phi246, i32 1
  N.load169 = load i32 from ptr %N.pinned
  170 = icmp slt i32 %182, i32 %N.load169
  br i1 %170, bb %while.body166, bb %while.exit167
while.exit167:  pred(while.exit145, while.body166)
  call void putch(i32 10)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getfarray(ptr )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


