global ptr N init with i32 0
global ptr L init with i32 0
global ptr M init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

i32 main() {
entry47:
  %c2 = stack 12
  %c1 = stack 12
  %c0 = stack 24
  %b2 = stack 12
  %b1 = stack 12
  %b0 = stack 12
  %a2 = stack 12
  %a1 = stack 12
  %a0 = stack 12
  store i32 3 to ptr @N
  store i32 3 to ptr @M
  store i32 3 to ptr @L
  br bb %while.cond
while.cond:
  i57.phi156 = phi [bb %entry47, i32 0, bb %while.body, i32 %96]
  M.load = load i32 from ptr @M
  63 = icmp slt i32 %i57.phi156, i32 %M.load
  br i1 %63, bb %while.body, bb %while.exit
while.body:
  160 = shl i32 %i57.phi156, i32 2
  67 = ptradd ptr %a0, i32 %160
  69 = si2f i32 %i57.phi156 to float
  store float %69 to ptr %67
  72 = ptradd ptr %a1, i32 %160
  store float %69 to ptr %72
  77 = ptradd ptr %a2, i32 %160
  store float %69 to ptr %77
  82 = ptradd ptr %b0, i32 %160
  store float %69 to ptr %82
  87 = ptradd ptr %b1, i32 %160
  store float %69 to ptr %87
  92 = ptradd ptr %b2, i32 %160
  store float %69 to ptr %92
  96 = add i32 %i57.phi156, i32 1
  br bb %while.cond
while.exit:
  3171 = ptradd ptr %c1, i32 8
  5172 = ptradd ptr %a2, i32 4
  5.load173 = load float from ptr %5172
  store float %5.load173 to ptr %3171
  8175 = ptradd ptr %c2, i32 4
  10176 = ptradd ptr %a1, i32 8
  10.load177 = load float from ptr %10176
  store float %10.load177 to ptr %8175
  13179 = ptradd ptr %c0, i32 4
  15.load180 = load float from ptr %a1
  store float %15.load180 to ptr %13179
  18182 = ptradd ptr %c0, i32 8
  20.load183 = load float from ptr %a2
  store float %20.load183 to ptr %18182
  25185 = ptradd ptr %a0, i32 4
  25.load186 = load float from ptr %25185
  store float %25.load186 to ptr %c1
  30188 = ptradd ptr %a0, i32 8
  30.load189 = load float from ptr %30188
  store float %30.load189 to ptr %c2
  33191 = ptradd ptr %c1, i32 4
  35192 = ptradd ptr %a1, i32 4
  35.load193 = load float from ptr %35192
  store float %35.load193 to ptr %33191
  38195 = ptradd ptr %c2, i32 8
  40196 = ptradd ptr %a2, i32 8
  40.load197 = load float from ptr %40196
  store float %40.load197 to ptr %38195
  45.load199 = load float from ptr %a0
  store float %45.load199 to ptr %c0
  br bb %while.cond99
while.cond99:
  i57.phi155 = phi [bb %while.exit, i32 0, bb %while.body100, i32 %114]
  N.load = load i32 from ptr @N
  104 = icmp slt i32 %i57.phi155, i32 %N.load
  br i1 %104, bb %while.body100, bb %while.exit101
while.body100:
  166 = shl i32 %i57.phi155, i32 2
  108 = ptradd ptr %c0, i32 %166
  108.load = load float from ptr %108
  110 = f2si float %108.load to i32
  call void putint(i32 %110)
  114 = add i32 %i57.phi155, i32 1
  br bb %while.cond99
while.exit101:
  call void putch(i32 10)
  br bb %while.cond117
while.cond117:
  i57.phi154 = phi [bb %while.exit101, i32 0, bb %while.body118, i32 %132]
  N.load121 = load i32 from ptr @N
  122 = icmp slt i32 %i57.phi154, i32 %N.load121
  br i1 %122, bb %while.body118, bb %while.exit119
while.body118:
  167 = shl i32 %i57.phi154, i32 2
  126 = ptradd ptr %c1, i32 %167
  126.load = load float from ptr %126
  128 = f2si float %126.load to i32
  call void putint(i32 %128)
  132 = add i32 %i57.phi154, i32 1
  br bb %while.cond117
while.exit119:
  call void putch(i32 10)
  br bb %while.cond135
while.cond135:
  i57.phi = phi [bb %while.exit119, i32 0, bb %while.body136, i32 %150]
  N.load139 = load i32 from ptr @N
  140 = icmp slt i32 %i57.phi, i32 %N.load139
  br i1 %140, bb %while.body136, bb %while.exit137
while.body136:
  168 = shl i32 %i57.phi, i32 2
  144 = ptradd ptr %c2, i32 %168
  144.load = load float from ptr %144
  146 = f2si float %144.load to i32
  call void putint(i32 %146)
  150 = add i32 %i57.phi, i32 1
  br bb %while.cond135
while.exit137:
  call void putch(i32 10)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getfarray(ptr )

i32 tran(ptr a0, ptr a1, ptr a2, ptr b0, ptr b1, ptr b2, ptr c0, ptr c1, ptr c2) {
entry:
  3 = ptradd ptr %c1, i32 8
  5 = ptradd ptr %a2, i32 4
  5.load = load float from ptr %5
  store float %5.load to ptr %3
  8 = ptradd ptr %c2, i32 4
  10 = ptradd ptr %a1, i32 8
  10.load = load float from ptr %10
  store float %10.load to ptr %8
  13 = ptradd ptr %c0, i32 4
  15.load = load float from ptr %a1
  store float %15.load to ptr %13
  18 = ptradd ptr %c0, i32 8
  20.load = load float from ptr %a2
  store float %20.load to ptr %18
  25 = ptradd ptr %a0, i32 4
  25.load = load float from ptr %25
  store float %25.load to ptr %c1
  30 = ptradd ptr %a0, i32 8
  30.load = load float from ptr %30
  store float %30.load to ptr %c2
  33 = ptradd ptr %c1, i32 4
  35 = ptradd ptr %a1, i32 4
  35.load = load float from ptr %35
  store float %35.load to ptr %33
  38 = ptradd ptr %c2, i32 8
  40 = ptradd ptr %a2, i32 8
  40.load = load float from ptr %40
  store float %40.load to ptr %38
  45.load = load float from ptr %a0
  store float %45.load to ptr %c0
  ret i32 0
}

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


