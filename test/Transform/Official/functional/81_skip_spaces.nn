i32 main() {
entry:  pred()
  %arr = stack 400
  758 = call i32 getint()
  859 = icmp ne i32 %758, i32 0
  br i1 %859, bb %while.body, bb %while.cond17_preheader
while.body:  pred(while.body, entry)
  i.phi3661 = phi [bb %while.body, i32 %16, bb %entry, i32 0]
  47 = shl i32 %i.phi3661, i32 2
  11 = ptradd ptr %arr, i32 %47
  13 = call i32 getint()
  store i32 %13 to ptr %11
  16 = add i32 %i.phi3661, i32 1
  7 = call i32 getint()
  8 = icmp ne i32 %7, i32 0
  br i1 %8, bb %while.body, bb %while.cond17_preheader
while.cond17_preheader:  pred(while.body, entry)
  60 = phi [bb %while.body, i32 %16, bb %entry, i32 0]
  2154 = icmp ne i32 %60, i32 0
  br i1 %2154, bb %while.body18, bb %while.exit19
while.body18:  pred(while.body18, while.cond17_preheader)
  i.phi57 = phi [bb %while.body18, i32 %39, bb %while.cond17_preheader, i32 %60]
  sum.phi56 = phi [bb %while.body18, i32 %31, bb %while.cond17_preheader, i32 0]
  39 = add i32 %i.phi57, i32 -1
  48 = shl i32 %i.phi57, i32 2
  73 = ptradd ptr %arr, i32 %48
  74 = ptradd ptr %73, i32 -4
  29.load = load i32 from ptr %74
  31 = add i32 %sum.phi56, i32 %29.load
  21 = icmp ne i32 %39, i32 0
  br i1 %21, bb %while.body18, bb %while.exit19
while.exit19:  pred(while.body18, while.cond17_preheader)
  sum.phi_ph_val55 = phi [bb %while.body18, i32 %31, bb %while.cond17_preheader, i32 0]
  33 = srem i32 %sum.phi_ph_val55, i32 79
  ret i32 %33
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


