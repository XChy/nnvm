i32 main() {
entry:
  %sum = stack 4
  %i = stack 4
  %arr = stack 400
  store i32 0 to ptr %i
  store i32 0 to ptr %sum
  br bb %while.cond
while.cond:
  7 = call i32 getint()
  8 = icmp ne i32 %7, i32 0
  br i1 %8, bb %while.body, bb %while.exit
while.body:
  i.load = load i32 from ptr %i
  10 = mul i32 %i.load, i32 4
  11 = ptradd ptr %arr, i32 %10
  12 = call i32 getint()
  store i32 %12 to ptr %11
  i.load13 = load i32 from ptr %i
  14 = add i32 %i.load13, i32 1
  store i32 %14 to ptr %i
  br bb %while.cond
while.exit:
  br bb %while.cond15
while.cond15:
  i.load18 = load i32 from ptr %i
  19 = icmp ne i32 %i.load18, i32 0
  br i1 %19, bb %while.body16, bb %while.exit17
while.body16:
  i.load20 = load i32 from ptr %i
  21 = sub i32 %i.load20, i32 1
  store i32 %21 to ptr %i
  sum.load = load i32 from ptr %sum
  24 = mul i32 %21, i32 4
  25 = ptradd ptr %arr, i32 %24
  25.load = load i32 from ptr %25
  27 = add i32 %sum.load, i32 %25.load
  store i32 %27 to ptr %sum
  br bb %while.cond15
while.exit17:
  sum.load28 = load i32 from ptr %sum
  29 = srem i32 %sum.load28, i32 79
  ret i32 %29
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


