i32 main() {
entry:  pred()
  %arr = stack 400
  743 = call i32 getint()
  844 = icmp ne i32 %743, i32 0
  br i1 %844, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry)
  br bb %while.body
while.body:  pred(while.body, while.body_preheader)
  i.phi3546 = phi [bb %while.body, i32 %16, bb %while.body_preheader, i32 0]
  37 = shl i32 %i.phi3546, i32 2
  11 = ptradd ptr %arr, i32 %37
  13 = call i32 getint()
  store i32 %13 to ptr %11
  16 = add i32 %i.phi3546, i32 1
  7 = call i32 getint()
  8 = icmp ne i32 %7, i32 0
  br i1 %8, bb %while.body, bb %while.exit
while.exit:  pred(while.body, entry)
  45 = phi [bb %while.body, i32 %16, bb %entry, i32 0]
  2139 = icmp ne i32 %45, i32 0
  br i1 %2139, bb %while.body18_preheader, bb %while.exit19
while.body18_preheader:  pred(while.exit)
  br bb %while.body18
while.body18:  pred(while.body18, while.body18_preheader)
  i.phi42 = phi [bb %while.body18, i32 %24, bb %while.body18_preheader, i32 %45]
  sum.phi41 = phi [bb %while.body18, i32 %31, bb %while.body18_preheader, i32 0]
  24 = sub i32 %i.phi42, i32 1
  38 = shl i32 %24, i32 2
  29 = ptradd ptr %arr, i32 %38
  29.load = load i32 from ptr %29
  31 = add i32 %sum.phi41, i32 %29.load
  21 = icmp ne i32 %24, i32 0
  br i1 %21, bb %while.body18, bb %while.exit19
while.exit19:  pred(while.body18, while.exit)
  40 = phi [bb %while.body18, i32 %31, bb %while.exit, i32 0]
  33 = srem i32 %40, i32 79
  ret i32 %33
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


