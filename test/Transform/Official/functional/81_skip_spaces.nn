i32 main() {
entry:  pred()
  %arr = stack 400
  753 = call i32 getint()
  854 = icmp ne i32 %753, i32 0
  br i1 %854, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry)
  br bb %while.body
while.body:  pred(while.body, while.body_preheader)
  i.phi3656 = phi [bb %while.body, i32 %16, bb %while.body_preheader, i32 0]
  47 = shl i32 %i.phi3656, i32 2
  11 = ptradd ptr %arr, i32 %47
  13 = call i32 getint()
  store i32 %13 to ptr %11
  16 = add i32 %i.phi3656, i32 1
  7 = call i32 getint()
  8 = icmp ne i32 %7, i32 0
  br i1 %8, bb %while.body, bb %while.body_exclu_exit
while.exit:  pred(entry, while.body_exclu_exit)
  55 = phi [bb %while.body_exclu_exit, i32 %16, bb %entry, i32 0]
  2149 = icmp ne i32 %55, i32 0
  br i1 %2149, bb %while.body18_preheader, bb %while.exit19
while.body18_preheader:  pred(while.exit)
  br bb %while.body18
while.body18:  pred(while.body18, while.body18_preheader)
  i.phi52 = phi [bb %while.body18, i32 %39, bb %while.body18_preheader, i32 %55]
  sum.phi51 = phi [bb %while.body18, i32 %31, bb %while.body18_preheader, i32 0]
  39 = add i32 %i.phi52, i32 -1
  48 = shl i32 %i.phi52, i32 2
  66 = ptradd ptr %arr, i32 %48
  67 = ptradd ptr %66, i32 -4
  29.load = load i32 from ptr %67
  31 = add i32 %sum.phi51, i32 %29.load
  21 = icmp ne i32 %39, i32 0
  br i1 %21, bb %while.body18, bb %while.body18_exclu_exit
while.exit19:  pred(while.exit, while.body18_exclu_exit)
  50 = phi [bb %while.body18_exclu_exit, i32 %31, bb %while.exit, i32 0]
  33 = srem i32 %50, i32 79
  ret i32 %33
while.body_exclu_exit:  pred(while.body)
  br bb %while.exit
while.body18_exclu_exit:  pred(while.body18)
  br bb %while.exit19
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


