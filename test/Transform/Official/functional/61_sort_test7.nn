internal global ptr buf init with 200 x i32 zeros
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry162:  pred()
  166 = call i32 getarray(ptr @buf)
  call void merge_sort(i32 0, i32 %166)
  call void putarray(i32 %166, ptr @buf)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )

internal void merge_sort(i32 l, i32 r) {
entry:  pred()
  4 = add i32 %l, i32 1
  6 = icmp sge i32 %4, i32 %r
  br i1 %6, bb %then, bb %if.exit
then:  pred(entry)
  ret 
if.exit:  pred(entry)
  12 = add i32 %l, i32 %r
  13 = sdiv i32 %12, i32 2
  call void merge_sort(i32 %l, i32 %13)
  call void merge_sort(i32 %13, i32 %r)
  br bb %while.cond
while.cond:  pred(if.exit, if.exit55)
  k.phi181 = phi [bb %if.exit, i32 %l, bb %if.exit55, i32 %89]
  j.phi178 = phi [bb %if.exit, i32 %13, bb %if.exit55, i32 %j.phi177]
  i.phi175 = phi [bb %if.exit, i32 %l, bb %if.exit55, i32 %i.phi174]
  31 = icmp slt i32 %i.phi175, i32 %13
  br i1 %31, bb %select.then, bb %select.else
while.body:  pred(select.exit)
  216 = shl i32 %i.phi175, i32 2
  45 = ptradd ptr @buf, i32 %216
  45.load = load i32 from ptr %45
  217 = shl i32 %j.phi178, i32 2
  51 = ptradd ptr @buf, i32 %217
  51.load = load i32 from ptr %51
  53 = icmp slt i32 %45.load, i32 %51.load
  218 = shl i32 %k.phi181, i32 2
  br i1 %53, bb %then54, bb %else
while.exit:  pred(select.exit)
  br i1 %31, bb %while.body91_preheader, bb %while.exit92
select.then:  pred(while.cond)
  38 = icmp slt i32 %j.phi178, i32 %r
  br bb %select.exit
select.else:  pred(while.cond)
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 0, bb %select.then, i1 %38]
  br i1 %select.phi, bb %while.body, bb %while.exit
then54:  pred(while.body)
  188 = ptradd ptr @buf, i32 %218
  189 = ptradd ptr %188, i32 400
  67.load = load i32 from ptr %45
  store i32 %67.load to ptr %189
  71 = add i32 %i.phi175, i32 1
  br bb %if.exit55
if.exit55:  pred(then54, else)
  j.phi177 = phi [bb %else, i32 %86, bb %then54, i32 %j.phi178]
  i.phi174 = phi [bb %else, i32 %i.phi175, bb %then54, i32 %71]
  89 = add i32 %k.phi181, i32 1
  br bb %while.cond
else:  pred(while.body)
  192 = ptradd ptr @buf, i32 %218
  193 = ptradd ptr %192, i32 400
  82.load = load i32 from ptr %51
  store i32 %82.load to ptr %193
  86 = add i32 %j.phi178, i32 1
  br bb %if.exit55
while.body91_preheader:  pred(while.exit)
  br bb %while.body91
while.body91:  pred(while.body91, while.body91_preheader)
  i.phi233 = phi [bb %while.body91, i32 %111, bb %while.body91_preheader, i32 %i.phi175]
  k.phi180232 = phi [bb %while.body91, i32 %114, bb %while.body91_preheader, i32 %k.phi181]
  220 = shl i32 %k.phi180232, i32 2
  196 = ptradd ptr @buf, i32 %220
  197 = ptradd ptr %196, i32 400
  221 = shl i32 %i.phi233, i32 2
  107 = ptradd ptr @buf, i32 %221
  107.load = load i32 from ptr %107
  store i32 %107.load to ptr %197
  111 = add i32 %i.phi233, i32 1
  114 = add i32 %k.phi180232, i32 1
  95 = icmp slt i32 %111, i32 %13
  br i1 %95, bb %while.body91, bb %while.body91_exclu_exit
while.exit92:  pred(while.exit, while.body91_exclu_exit)
  k.phi181231 = phi [bb %while.body91_exclu_exit, i32 %114, bb %while.exit, i32 %k.phi181]
  120227 = icmp slt i32 %j.phi178, i32 %r
  br i1 %120227, bb %while.body116_preheader, bb %while.exit117
while.body116_preheader:  pred(while.exit92)
  br bb %while.body116
while.body116:  pred(while.body116, while.body116_preheader)
  j.phi229 = phi [bb %while.body116, i32 %136, bb %while.body116_preheader, i32 %j.phi178]
  k.phi228 = phi [bb %while.body116, i32 %139, bb %while.body116_preheader, i32 %k.phi181231]
  222 = shl i32 %k.phi228, i32 2
  200 = ptradd ptr @buf, i32 %222
  201 = ptradd ptr %200, i32 400
  223 = shl i32 %j.phi229, i32 2
  132 = ptradd ptr @buf, i32 %223
  132.load = load i32 from ptr %132
  store i32 %132.load to ptr %201
  136 = add i32 %j.phi229, i32 1
  139 = add i32 %k.phi228, i32 1
  120 = icmp slt i32 %136, i32 %r
  br i1 %120, bb %while.body116, bb %while.exit117
while.exit117:  pred(while.exit92, while.body116)
  145225 = icmp slt i32 %l, i32 %r
  br i1 %145225, bb %while.body141_preheader, bb %while.exit142
while.body141_preheader:  pred(while.exit117)
  br bb %while.body141
while.body141:  pred(while.body141, while.body141_preheader)
  l.stack.phi226 = phi [bb %while.body141, i32 %161, bb %while.body141_preheader, i32 %l]
  224 = shl i32 %l.stack.phi226, i32 2
  151 = ptradd ptr @buf, i32 %224
  206 = ptradd ptr %151, i32 400
  157.load = load i32 from ptr %206
  store i32 %157.load to ptr %151
  161 = add i32 %l.stack.phi226, i32 1
  145 = icmp slt i32 %161, i32 %r
  br i1 %145, bb %while.body141, bb %while.exit142
while.exit142:  pred(while.exit117, while.body141)
  ret 
while.body91_exclu_exit:  pred(while.body91)
  br bb %while.exit92
}


