external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry24:
  br bb %whileIf.while.cond
whileIf.while.cond:
  b.phi2934 = phi [bb %entry24, i32 0, bb %whileIf.if.exit, i32 %b.phi2846]
  a.phi35 = phi [bb %entry24, i32 0, bb %whileIf.if.exit, i32 %2247]
  736 = icmp slt i32 %a.phi35, i32 100
  br i1 %736, bb %whileIf.while.body, bb %whileIf.while.exit
whileIf.while.body:
  1039 = icmp eq i32 %a.phi35, i32 5
  br i1 %1039, bb %whileIf.then, bb %whileIf.else
whileIf.while.exit:
  ret i32 %b.phi2934
whileIf.then:
  br bb %whileIf.if.exit
whileIf.if.exit:
  b.phi2846 = phi [bb %whileIf.if.exit17, i32 %b.phi55, bb %whileIf.then, i32 25]
  2247 = add i32 %a.phi35, i32 1
  br bb %whileIf.while.cond
whileIf.else:
  1550 = icmp eq i32 %a.phi35, i32 10
  br i1 %1550, bb %whileIf.then16, bb %whileIf.else18
whileIf.then16:
  br bb %whileIf.if.exit17
whileIf.if.exit17:
  b.phi55 = phi [bb %whileIf.else18, i32 %2058, bb %whileIf.then16, i32 42]
  br bb %whileIf.if.exit
whileIf.else18:
  2058 = mul i32 %a.phi35, i32 2
  br bb %whileIf.if.exit17
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )

pure i32 whileIf() {
entry:
  br bb %while.cond
while.cond:
  b.phi29 = phi [bb %entry, i32 0, bb %if.exit, i32 %b.phi28]
  a.phi = phi [bb %entry, i32 0, bb %if.exit, i32 %22]
  7 = icmp slt i32 %a.phi, i32 100
  br i1 %7, bb %while.body, bb %while.exit
while.body:
  10 = icmp eq i32 %a.phi, i32 5
  br i1 %10, bb %then, bb %else
while.exit:
  ret i32 %b.phi29
then:
  br bb %if.exit
if.exit:
  b.phi28 = phi [bb %if.exit17, i32 %b.phi, bb %then, i32 25]
  22 = add i32 %a.phi, i32 1
  br bb %while.cond
else:
  15 = icmp eq i32 %a.phi, i32 10
  br i1 %15, bb %then16, bb %else18
then16:
  br bb %if.exit17
if.exit17:
  b.phi = phi [bb %else18, i32 %20, bb %then16, i32 42]
  br bb %if.exit
else18:
  20 = mul i32 %a.phi, i32 2
  br bb %if.exit17
}


