external i32 getint()

external i32 getch()

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry38:  pred()
  40 = call i32 getint()
  4569 = icmp sgt i32 %40, i32 0
  br i1 %4569, bb %while.body, bb %while.exit
while.cond:  pred(while.body)
  n.phi = phi [bb %while.body, i32 %51]
  45 = icmp sgt i32 %n.phi, i32 0
  br i1 %45, bb %while.body, bb %while.exit
while.body:  pred(while.cond, entry38)
  n.phi70 = phi [bb %entry38, i32 %40, bb %while.cond, i32 %n.phi]
  47 = call i32 getint()
  call void hanoi(i32 %47, i32 1, i32 2, i32 3)
  call void putch(i32 10)
  51 = sub i32 %n.phi70, i32 1
  br bb %while.cond
while.exit:  pred(while.cond, entry38)
  ret i32 0
}

void hanoi(i32 n, i32 one, i32 two, i32 three) {
entry10:  pred()
  16 = icmp eq i32 %n, i32 1
  br i1 %16, bb %then, bb %else
then:  pred(entry10)
  call void putint(i32 %one)
  call void putch(i32 32)
  call void putint(i32 %three)
  call void putch(i32 44)
  call void putch(i32 32)
  br bb %if.exit
if.exit:  pred(else, then)
  ret 
else:  pred(entry10)
  24 = sub i32 %n, i32 1
  call void hanoi(i32 %24, i32 %one, i32 %three, i32 %two)
  call void putint(i32 %one)
  call void putch(i32 32)
  call void putint(i32 %three)
  call void putch(i32 44)
  call void putch(i32 32)
  call void hanoi(i32 %24, i32 %two, i32 %one, i32 %three)
  br bb %if.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )

external void putint(i32 )

void move(i32 x, i32 y) {
entry:  pred()
  call void putint(i32 %x)
  call void putch(i32 32)
  call void putint(i32 %y)
  call void putch(i32 44)
  call void putch(i32 32)
  ret 
}


