global ptr k init with i32 0
i32 main() {
entry:  pred()
  store i32 3389 to ptr @k
  store i32 3390 to ptr @k
  1252 = icmp sgt i32 112, i32 10
  br i1 %1252, bb %while.body_preheader, bb %while.exit
while.cond:  pred(if.exit19)
  g25.phi44 = phi [bb %if.exit19, i32 %g25.phi]
  l.phi41 = phi [bb %if.exit19, i32 %l.phi]
  g.phi38 = phi [bb %if.exit19, i32 %g.phi]
  k.phi35 = phi [bb %if.exit19, i32 %k.phi]
  12 = icmp sgt i32 %k.phi35, i32 10
  br i1 %12, bb %while.body, bb %while.exit
while.body_preheader:  pred(entry)
  g25.phi4454_ph_val = phi [bb %entry, i32 0]
  l.phi4155_ph_val = phi [bb %entry, i32 0]
  g.phi3856_ph_val = phi [bb %entry, i32 0]
  k.phi3557_ph_val = phi [bb %entry, i32 112]
  br bb %while.body
while.body:  pred(while.cond, while.body_preheader)
  k.phi3557 = phi [bb %while.cond, i32 %k.phi35, bb %while.body_preheader, i32 %k.phi3557_ph_val]
  g.phi3856 = phi [bb %while.cond, i32 %g.phi38, bb %while.body_preheader, i32 %g.phi3856_ph_val]
  l.phi4155 = phi [bb %while.cond, i32 %l.phi41, bb %while.body_preheader, i32 %l.phi4155_ph_val]
  g25.phi4454 = phi [bb %while.cond, i32 %g25.phi44, bb %while.body_preheader, i32 %g25.phi4454_ph_val]
  15 = sub i32 %k.phi3557, i32 88
  17 = icmp slt i32 %15, i32 1000
  br i1 %17, bb %then18, bb %if.exit19
while.exit:  pred(while.cond, entry)
  53 = phi [bb %while.cond, i32 %k.phi35, bb %entry, i32 112]
  call void putint(i32 %53)
  k.load33 = load i32 from ptr @k
  ret i32 %k.load33
then18:  pred(while.body)
  51 = add i32 %k.phi3557, i32 -76
  br bb %if.exit19
if.exit19:  pred(while.body, then18)
  g25.phi = phi [bb %while.body, i32 %g25.phi4454, bb %then18, i32 11]
  l.phi = phi [bb %while.body, i32 %l.phi4155, bb %then18, i32 11]
  g.phi = phi [bb %while.body, i32 %g.phi3856, bb %then18, i32 10]
  k.phi = phi [bb %while.body, i32 %15, bb %then18, i32 %51]
  br bb %while.cond
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


