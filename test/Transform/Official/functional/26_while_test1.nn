external i32 getint()

pure i32 doubleWhile() {
entry:
  br bb %while.cond
while.cond:
  j.phi = phi [bb %entry, i32 7, bb %while.exit13, i32 %20]
  i.phi = phi [bb %entry, i32 5, bb %while.exit13, i32 %10]
  7 = icmp slt i32 %i.phi, i32 100
  br i1 %7, bb %while.body, bb %while.exit
while.body:
  10 = add i32 %i.phi, i32 30
  br bb %while.cond11
while.exit:
  ret i32 %j.phi
while.cond11:
  j.phi26 = phi [bb %while.body, i32 %j.phi, bb %while.body12, i32 %18]
  15 = icmp slt i32 %j.phi26, i32 100
  br i1 %15, bb %while.body12, bb %while.exit13
while.body12:
  18 = add i32 %j.phi26, i32 6
  br bb %while.cond11
while.exit13:
  20 = sub i32 %j.phi26, i32 100
  br bb %while.cond
}

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

pure i32 main() {
entry22:
  br bb %doubleWhile.while.cond
doubleWhile.while.cond:
  j.phi31 = phi [bb %entry22, i32 7, bb %doubleWhile.while.exit13, i32 %2048]
  i.phi32 = phi [bb %entry22, i32 5, bb %doubleWhile.while.exit13, i32 %1036]
  733 = icmp slt i32 %i.phi32, i32 100
  br i1 %733, bb %doubleWhile.while.body, bb %doubleWhile.while.exit
doubleWhile.while.body:
  1036 = add i32 %i.phi32, i32 30
  br bb %doubleWhile.while.cond11
doubleWhile.while.exit:
  ret i32 %j.phi31
doubleWhile.while.cond11:
  j.phi2641 = phi [bb %doubleWhile.while.body, i32 %j.phi31, bb %doubleWhile.while.body12, i32 %1845]
  1542 = icmp slt i32 %j.phi2641, i32 100
  br i1 %1542, bb %doubleWhile.while.body12, bb %doubleWhile.while.exit13
doubleWhile.while.body12:
  1845 = add i32 %j.phi2641, i32 6
  br bb %doubleWhile.while.cond11
doubleWhile.while.exit13:
  2048 = sub i32 %j.phi2641, i32 100
  br bb %doubleWhile.while.cond
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


