external i32 getint()

i32 doubleWhile() {
entry:
  br bb %while.cond
while.cond:
  j.phi = phi [bb %entry, i32 7, bb %while.exit13, i32 %20]
  i.phi = phi [bb %entry, i32 5, bb %while.exit13, i32 %10]
  7 = icmp slt i32 %i.phi, i32 100
  br i1 %7, bb %while.body, bb %while.exit
while.body:
  10 = add i32 %i.phi, i32 30
  br bb %while.cond11
while.exit:
  ret i32 %j.phi
while.cond11:
  j.phi26 = phi [bb %while.body, i32 %j.phi, bb %while.body12, i32 %18]
  15 = icmp slt i32 %j.phi26, i32 100
  br i1 %15, bb %while.body12, bb %while.exit13
while.body12:
  18 = add i32 %j.phi26, i32 6
  br bb %while.cond11
while.exit13:
  20 = sub i32 %j.phi26, i32 100
  br bb %while.cond
}

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry22:
  23 = call i32 doubleWhile()
  ret i32 %23
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


