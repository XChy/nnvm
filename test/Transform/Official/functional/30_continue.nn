i32 main() {
entry:
  br bb %while.cond
while.cond:
  22 = phi [bb %entry, i32 0, bb %if.exit, i32 %17, bb %then, i32 %22]
  21 = phi [bb %entry, i32 0, bb %if.exit, i32 %19, bb %then, i32 %14]
  7 = icmp slt i32 %21, i32 100
  8 = icmp ne i1 %7, i1 0
  br i1 %8, bb %while.body, bb %while.exit
while.body:
  10 = icmp eq i32 %21, i32 50
  br i1 %10, bb %then, bb %if.exit
while.exit:
  ret i32 %22
then:
  14 = add i32 %21, i32 1
  br bb %while.cond
if.exit:
  17 = add i32 %22, i32 %21
  19 = add i32 %21, i32 1
  br bb %while.cond
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


