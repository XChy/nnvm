global ptr b init with i32 0
global ptr a init with i32 0
i32 main() {
entry:
  %select = stack 1
  %c = stack 4
  1 = call i32 getint()
  store i32 %1 to ptr @a
  2 = call i32 getint()
  store i32 %2 to ptr @b
  a.load = load i32 from ptr @a
  6 = icmp eq i32 %a.load, i32 %2
  br i1 %6, bb %select.then, bb %select.else
select.then:
  a.load11 = load i32 from ptr @a
  12 = icmp ne i32 %a.load11, i32 3
  store i1 %12 to ptr %select
  br bb %select.exit
select.else:
  store i1 0 to ptr %select
  br bb %select.exit
select.exit:
  13 = load i1 from ptr %select
  br i1 %13, bb %then, bb %else
then:
  store i32 1 to ptr %c
  br bb %if.exit
if.exit:
  c.load = load i32 from ptr %c
  ret i32 %c.load
else:
  store i32 0 to ptr %c
  br bb %if.exit
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


