global ptr k init with 1 x i32 zeros
global ptr i init with 1 x i32 zeros
void sub(ptr x, ptr y) {
entry82:
  84 = ptradd ptr @k, i32 0
  84.load = load i32 from ptr %84
  call void sub_impl(ptr %x, ptr %y, i32 %84.load)
  ret 
}

void sub_impl(ptr x, ptr y, i32 rec) {
entry57:
  60 = icmp eq i32 %rec, i32 0
  br i1 %60, bb %then61, bb %else63
then61:
  65 = ptradd ptr %x, i32 0
  67.load = load i32 from ptr %65
  70 = ptradd ptr %y, i32 0
  70.load = load i32 from ptr %70
  72 = sub i32 %67.load, i32 %70.load
  store i32 %72 to ptr %65
  br bb %if.exit62
if.exit62:
  ret 
else63:
  74 = ptradd ptr %x, i32 0
  76.load = load i32 from ptr %74
  78 = mul i32 %76.load, i32 2
  store i32 %78 to ptr %74
  80 = sub i32 %rec, i32 1
  call void sub_impl(ptr %x, ptr %y, i32 %80)
  br bb %if.exit62
}

void inc(ptr x) {
entry22:
  24 = ptradd ptr @k, i32 0
  24.load = load i32 from ptr %24
  call void inc_impl(ptr %x, i32 %24.load)
  ret 
}

external i32 getint()

external i32 getch()

external void putint(i32 )

void add(ptr x, ptr y) {
entry52:
  54 = ptradd ptr @k, i32 0
  54.load = load i32 from ptr %54
  call void add_impl(ptr %x, ptr %y, i32 %54.load)
  ret 
}

external i32 getarray(ptr )

void add_impl(ptr x, ptr y, i32 rec) {
entry27:
  30 = icmp eq i32 %rec, i32 0
  br i1 %30, bb %then31, bb %else33
then31:
  35 = ptradd ptr %x, i32 0
  37.load = load i32 from ptr %35
  40 = ptradd ptr %y, i32 0
  40.load = load i32 from ptr %40
  42 = add i32 %37.load, i32 %40.load
  store i32 %42 to ptr %35
  br bb %if.exit32
if.exit32:
  ret 
else33:
  44 = ptradd ptr %x, i32 0
  46.load = load i32 from ptr %44
  48 = mul i32 %46.load, i32 2
  store i32 %48 to ptr %44
  50 = sub i32 %rec, i32 1
  call void add_impl(ptr %x, ptr %y, i32 %50)
  br bb %if.exit32
}

external i32 getfarray(ptr )

i32 main() {
entry87:
  %z = stack 8
  %y = stack 4
  %j = stack 4
  arr.index = ptradd ptr %z, i32 0
  store i32 -1 to ptr %arr.index
  arr.index93 = ptradd ptr %z, i32 4
  store i32 0 to ptr %arr.index93
  95 = ptradd ptr @k, i32 0
  96 = call i32 getint()
  store i32 %96 to ptr %95
  98 = ptradd ptr %y, i32 0
  99 = call i32 getint()
  store i32 %99 to ptr %98
  102 = call i32 getarray(ptr %arr.index)
  br bb %while.cond
while.cond:
  107.load = load i32 from ptr %98
  109 = icmp ne i32 %107.load, i32 0
  br i1 %109, bb %while.body, bb %while.exit
while.body:
  111 = ptradd ptr %j, i32 0
  115 = ptradd ptr %arr.index, i32 0
  115.load = load i32 from ptr %115
  store i32 %115.load to ptr %111
  br bb %while.cond117
while.exit:
  call void putch(i32 10)
  ret i32 0
while.cond117:
  121.load = load i32 from ptr %111
  123 = icmp slt i32 %121.load, i32 5
  br i1 %123, bb %while.body118, bb %while.exit119
while.body118:
  126 = ptradd ptr @i, i32 0
  126.load = load i32 from ptr %126
  call void putint(i32 %126.load)
  130.load = load i32 from ptr %111
  call void putint(i32 %130.load)
  134.load = load i32 from ptr %98
  call void putint(i32 %134.load)
  140.load = load i32 from ptr %115
  call void putint(i32 %140.load)
  call void add(ptr %arr.index, ptr %y)
  call void add(ptr %j, ptr %y)
  call void sub(ptr %arr.index, ptr %y)
  br bb %while.cond117
while.exit119:
  call void inc(ptr @i)
  call void add(ptr @i, ptr %arr.index)
  155 = ptradd ptr @i, i32 0
  155.load = load i32 from ptr %155
  160 = ptradd ptr %arr.index, i32 4
  160.load = load i32 from ptr %160
  162 = icmp eq i32 %155.load, i32 %160.load
  br i1 %162, bb %then163, bb %if.exit164
then163:
  br bb %while.exit
if.exit164:
  br bb %while.cond
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

void inc_impl(ptr x, i32 rec) {
entry:
  3 = icmp eq i32 %rec, i32 0
  br i1 %3, bb %then, bb %else
then:
  8 = ptradd ptr %x, i32 0
  10.load = load i32 from ptr %8
  12 = add i32 %10.load, i32 1
  store i32 %12 to ptr %8
  br bb %if.exit
if.exit:
  ret 
else:
  14 = ptradd ptr %x, i32 0
  16.load = load i32 from ptr %14
  18 = mul i32 %16.load, i32 2
  store i32 %18 to ptr %14
  20 = sub i32 %rec, i32 1
  call void inc_impl(ptr %x, i32 %20)
  br bb %if.exit
}


