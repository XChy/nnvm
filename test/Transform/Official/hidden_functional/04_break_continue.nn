i32 main() {
entry:
  br bb %while.cond
while.cond:
  n.phi114 = phi [bb %entry, i32 ub, bb %while.exit12, i32 %n.phi113]
  m.phi109 = phi [bb %entry, i32 ub, bb %while.exit12, i32 %m.phi108]
  sum.phi105 = phi [bb %entry, i32 0, bb %while.exit12, i32 %sum.phi104]
  select.phi99 = phi [bb %entry, i1 ub, bb %while.exit12, i1 %select.phi98]
  k.phi93 = phi [bb %entry, i32 ub, bb %while.exit12, i32 %k.phi92]
  i.phi = phi [bb %entry, i32 0, bb %while.exit12, i32 %86]
  7 = icmp slt i32 %i.phi, i32 20
  8 = icmp ne i1 %7, i1 0
  br i1 %8, bb %while.body, bb %while.exit
while.body:
  br bb %while.cond10
while.exit:
  ret i32 %sum.phi105
while.cond10:
  n.phi113 = phi [bb %while.body, i32 %n.phi114, bb %while.exit19, i32 %n.phi112]
  m.phi108 = phi [bb %while.body, i32 %m.phi109, bb %while.exit19, i32 %m.phi107]
  sum.phi104 = phi [bb %while.body, i32 %sum.phi105, bb %while.exit19, i32 %sum.phi103]
  select.phi98 = phi [bb %while.body, i1 %select.phi99, bb %while.exit19, i1 %select.phi97]
  k.phi92 = phi [bb %while.body, i32 %k.phi93, bb %while.exit19, i32 %k.phi]
  j.phi = phi [bb %while.body, i32 0, bb %while.exit19, i32 %84]
  14 = icmp slt i32 %j.phi, i32 10
  15 = icmp ne i1 %14, i1 0
  br i1 %15, bb %while.body11, bb %while.exit12
while.body11:
  br bb %while.cond17
while.exit12:
  86 = add i32 %i.phi, i32 1
  br bb %while.cond
while.cond17:
  n.phi112 = phi [bb %while.body11, i32 %n.phi113, bb %while.exit75, i32 %n.phi111]
  m.phi107 = phi [bb %while.body11, i32 %m.phi108, bb %while.exit75, i32 %m.phi]
  sum.phi103 = phi [bb %while.body11, i32 %sum.phi104, bb %while.exit75, i32 %sum.phi]
  select.phi97 = phi [bb %while.body11, i1 %select.phi98, bb %while.exit75, i1 %select.phi96]
  k.phi = phi [bb %while.body11, i32 0, bb %while.exit75, i32 %82]
  21 = icmp slt i32 %k.phi, i32 5
  22 = icmp ne i1 %21, i1 0
  br i1 %22, bb %while.body18, bb %while.exit19
while.body18:
  br bb %while.cond24
while.exit19:
  84 = add i32 %j.phi, i32 1
  br bb %while.cond10
while.cond24:
  n.phi111 = phi [bb %while.body18, i32 %n.phi112, bb %while.exit63, i32 %n.phi]
  m.phi = phi [bb %while.body18, i32 0, bb %while.exit63, i32 %70]
  sum.phi = phi [bb %while.body18, i32 %sum.phi103, bb %while.exit63, i32 %72]
  select.phi101 = phi [bb %while.body18, i1 %select.phi97, bb %while.exit63, i1 %select.phi100]
  28 = icmp slt i32 %m.phi, i32 3
  29 = icmp ne i1 %28, i1 0
  br i1 %29, bb %while.body25, bb %while.exit26
while.body25:
  31 = add i32 %m.phi, i32 1
  32 = icmp sge i32 %31, i32 3
  br i1 %32, bb %then, bb %if.exit
while.exit26:
  select.phi96 = phi [bb %while.cond24, i1 %select.phi101, bb %then58, i1 %select.phi]
  br bb %while.cond73
then:
  36 = icmp ne i32 %m.phi, i32 0
  br i1 %36, bb %then37, bb %if.exit38
if.exit:
  select.phi100 = phi [bb %while.body25, i1 %select.phi101, bb %if.exit38, i1 %select.phi95]
  br bb %while.cond61
then37:
  40 = icmp ne i32 %m.phi, i32 0
  br i1 %40, bb %select.then, bb %select.else
if.exit38:
  select.phi95 = phi [bb %then, i1 %select.phi101, bb %if.exit53, i1 %select.phi]
  br bb %if.exit
select.then:
  br bb %select.exit
select.else:
  46 = icmp ne i32 %m.phi, i32 0
  47 =  i1 %46, i1 1
  48 = zext i1 %47 to i32
  49 = icmp ne i32 %48, i32 0
  50 = icmp ne i1 %49, i1 0
  br bb %select.exit
select.exit:
  select.phi = phi [bb %select.else, i1 %50, bb %select.then, i1 1]
  br i1 %select.phi, bb %then52, bb %if.exit53
then52:
  55 = sub i32 0, i32 1
  56 = sub i32 %m.phi, i32 %55
  57 = icmp sge i32 %56, i32 3
  br i1 %57, bb %then58, bb %if.exit59
if.exit53:
  br bb %if.exit38
then58:
  br bb %while.exit26
if.exit59:
  br bb %if.exit53
while.cond61:
  n.phi = phi [bb %if.exit, i32 0, bb %while.body62, i32 %68]
  65 = icmp slt i32 %n.phi, i32 2
  66 = icmp ne i1 %65, i1 0
  br i1 %66, bb %while.body62, bb %while.exit63
while.body62:
  68 = add i32 %n.phi, i32 1
  br bb %while.cond61
while.exit63:
  70 = add i32 %m.phi, i32 1
  72 = add i32 %sum.phi, i32 1
  br bb %while.cond24
while.cond73:
  76 = icmp ne i32 1, i32 0
  br i1 %76, bb %while.body74, bb %while.exit75
while.body74:
  br bb %while.cond77
while.exit75:
  82 = add i32 %k.phi, i32 1
  br bb %while.cond17
while.cond77:
  80 = icmp ne i32 1, i32 0
  br i1 %80, bb %while.body78, bb %while.exit79
while.body78:
  br bb %while.exit79
while.exit79:
  br bb %while.exit75
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


