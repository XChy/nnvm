pure i32 main() {
entry:  pred()
  br bb %while.body
while.body:  pred(while.body11_exclu_exit, entry)
  sum.phi98159 = phi [bb %while.body11_exclu_exit, i32 %sum.phi, bb %entry, i32 0]
  i.phi158 = phi [bb %while.body11_exclu_exit, i32 %92, bb %entry, i32 0]
  select.phi114155 = phi [bb %while.body11_exclu_exit, i1 %select.phi111, bb %entry, i1 0]
  n.phi121154 = phi [bb %while.body11_exclu_exit, i32 %n.phi118, bb %entry, i32 0]
  br bb %while.body11
while.body11:  pred(while.body18_exclu_exit, while.body)
  sum.phi97151 = phi [bb %while.body18_exclu_exit, i32 %sum.phi, bb %while.body, i32 %sum.phi98159]
  j.phi150 = phi [bb %while.body18_exclu_exit, i32 %89, bb %while.body, i32 0]
  select.phi113147 = phi [bb %while.body18_exclu_exit, i1 %select.phi111, bb %while.body, i1 %select.phi114155]
  n.phi120146 = phi [bb %while.body18_exclu_exit, i32 %n.phi118, bb %while.body, i32 %n.phi121154]
  br bb %while.body18
while.body18:  pred(while.exit26, while.body11)
  sum.phi96139 = phi [bb %while.exit26, i32 %sum.phi, bb %while.body11, i32 %sum.phi97151]
  k.phi138 = phi [bb %while.exit26, i32 %86, bb %while.body11, i32 0]
  select.phi112136 = phi [bb %while.exit26, i1 %select.phi111, bb %while.body11, i1 %select.phi113147]
  n.phi119135 = phi [bb %while.exit26, i32 %n.phi118, bb %while.body11, i32 %n.phi120146]
  br bb %while.cond24
while.cond24:  pred(while.body18, if.exit)
  n.phi118 = phi [bb %while.body18, i32 %n.phi119135, bb %if.exit, i32 2]
  select.phi116 = phi [bb %while.body18, i1 %select.phi112136, bb %if.exit, i1 %select.phi115]
  m.phi = phi [bb %while.body18, i32 0, bb %if.exit, i32 %31]
  sum.phi = phi [bb %while.body18, i32 %sum.phi96139, bb %if.exit, i32 %75]
  28 = icmp slt i32 %m.phi, i32 3
  br i1 %28, bb %while.body25, bb %while.exit26
while.body25:  pred(while.cond24)
  31 = add i32 %m.phi, i32 1
  32 = icmp sge i32 %31, i32 3
  br i1 %32, bb %then, bb %if.exit
while.exit26:  pred(while.cond24, then58)
  select.phi111 = phi [bb %while.cond24, i1 %select.phi116, bb %then58, i1 %125]
  86 = add i32 %k.phi138, i32 1
  21 = icmp slt i32 %86, i32 5
  br i1 %21, bb %while.body18, bb %while.body18_exclu_exit
then:  pred(while.body25)
  36 = icmp ne i32 %m.phi, i32 0
  br i1 %36, bb %then37, bb %if.exit38
if.exit:  pred(while.body25, if.exit38)
  select.phi115 = phi [bb %while.body25, i1 %select.phi116, bb %if.exit38, i1 %select.phi110]
  75 = add i32 %sum.phi, i32 1
  br bb %while.cond24
then37:  pred(then)
  47 = xor i1 %36, i1 1
  125 = or i1 %36, i1 %47
  br i1 %125, bb %then52, bb %if.exit53
if.exit38:  pred(then, if.exit53)
  select.phi110 = phi [bb %then, i1 %select.phi116, bb %if.exit53, i1 %125]
  br bb %if.exit
then52:  pred(then37)
  br i1 %32, bb %then58, bb %if.exit53
if.exit53:  pred(then37, then52)
  br bb %if.exit38
then58:  pred(then52)
  br bb %while.exit26
while.body18_exclu_exit:  pred(while.exit26)
  89 = add i32 %j.phi150, i32 1
  14 = icmp slt i32 %89, i32 10
  br i1 %14, bb %while.body11, bb %while.body11_exclu_exit
while.body11_exclu_exit:  pred(while.body18_exclu_exit)
  92 = add i32 %i.phi158, i32 1
  7 = icmp slt i32 %92, i32 20
  br i1 %7, bb %while.body, bb %while.body_exclu_exit
while.body_exclu_exit:  pred(while.body11_exclu_exit)
  ret i32 %sum.phi
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


