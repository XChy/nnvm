pure i32 main() {
entry:  pred()
  br bb %while.cond10_preheader
while.exit:  pred(while.exit12)
  ret i32 %sum.phi
while.cond10_preheader:  pred(while.exit12, entry)
  sum.phi118282 = phi [bb %while.exit12, i32 %sum.phi, bb %entry, i32 0]
  i.phi281 = phi [bb %while.exit12, i32 %105, bb %entry, i32 0]
  select.phi170279 = phi [bb %while.exit12, i1 %select.phi163, bb %entry, i1 0]
  n.phi200277 = phi [bb %while.exit12, i32 %n.phi190, bb %entry, i32 0]
  br bb %while.cond17_preheader
while.exit12:  pred(while.exit19)
  105 = add i32 %i.phi124, i32 1
  7 = icmp slt i32 %105, i32 20
  br i1 %7, bb %while.cond10_preheader, bb %while.exit
while.cond17_preheader:  pred(while.exit19, while.cond10_preheader)
  sum.phi117274 = phi [bb %while.exit19, i32 %sum.phi, bb %while.cond10_preheader, i32 %sum.phi118282]
  i.phi122273 = phi [bb %while.exit19, i32 %i.phi124, bb %while.cond10_preheader, i32 %i.phi281]
  j.phi272 = phi [bb %while.exit19, i32 %98, bb %while.cond10_preheader, i32 0]
  select.phi169270 = phi [bb %while.exit19, i1 %select.phi163, bb %while.cond10_preheader, i1 %select.phi170279]
  n.phi199268 = phi [bb %while.exit19, i32 %n.phi190, bb %while.cond10_preheader, i32 %n.phi200277]
  br bb %while.cond24_preheader
while.exit19:  pred(while.exit26)
  98 = add i32 %j.phi137, i32 1
  14 = icmp slt i32 %98, i32 10
  br i1 %14, bb %while.cond17_preheader, bb %while.exit12
while.cond24_preheader:  pred(while.exit26, while.cond17_preheader)
  sum.phi116260 = phi [bb %while.exit26, i32 %sum.phi, bb %while.cond17_preheader, i32 %sum.phi117274]
  i.phi123259 = phi [bb %while.exit26, i32 %i.phi124, bb %while.cond17_preheader, i32 %i.phi122273]
  j.phi135258 = phi [bb %while.exit26, i32 %j.phi137, bb %while.cond17_preheader, i32 %j.phi272]
  k.phi257 = phi [bb %while.exit26, i32 %95, bb %while.cond17_preheader, i32 0]
  select.phi167256 = phi [bb %while.exit26, i1 %select.phi163, bb %while.cond17_preheader, i1 %select.phi169270]
  n.phi198254 = phi [bb %while.exit26, i32 %n.phi190, bb %while.cond17_preheader, i32 %n.phi199268]
  br bb %while.cond24
while.cond24:  pred(while.body64.iter1, while.cond24_preheader)
  n.phi190 = phi [bb %while.body64.iter1, i32 2, bb %while.cond24_preheader, i32 %n.phi198254]
  m.phi = phi [bb %while.body64.iter1, i32 %31, bb %while.cond24_preheader, i32 0]
  select.phi168 = phi [bb %while.body64.iter1, i1 %select.phi171, bb %while.cond24_preheader, i1 %select.phi167256]
  k.phi148 = phi [bb %while.body64.iter1, i32 %k.phi158242356, bb %while.cond24_preheader, i32 %k.phi257]
  j.phi137 = phi [bb %while.body64.iter1, i32 %j.phi145243355, bb %while.cond24_preheader, i32 %j.phi135258]
  i.phi124 = phi [bb %while.body64.iter1, i32 %i.phi132244354, bb %while.cond24_preheader, i32 %i.phi123259]
  sum.phi = phi [bb %while.body64.iter1, i32 %82, bb %while.cond24_preheader, i32 %sum.phi116260]
  28 = icmp slt i32 %m.phi, i32 3
  br i1 %28, bb %while.body25, bb %while.exit26
while.body25:  pred(while.cond24)
  31 = add i32 %m.phi, i32 1
  32 = icmp sge i32 %31, i32 3
  br i1 %32, bb %then, bb %if.exit
while.exit26:  pred(while.cond24, then52)
  select.phi163 = phi [bb %while.cond24, i1 %select.phi168, bb %then52, i1 %204]
  95 = add i32 %k.phi148, i32 1
  21 = icmp slt i32 %95, i32 5
  br i1 %21, bb %while.cond24_preheader, bb %while.exit19
then:  pred(while.body25)
  36 = icmp ne i32 %m.phi, i32 0
  br i1 %36, bb %then37, bb %if.exit38
if.exit:  pred(while.body25, if.exit38)
  select.phi171 = phi [bb %while.body25, i1 %select.phi168, bb %if.exit38, i1 %select.phi172]
  br bb %while.body64.iter1
then37:  pred(then)
  47 = xor i1 %36, i1 1
  204 = or i1 %36, i1 %47
  br i1 %204, bb %then52, bb %if.exit53
if.exit38:  pred(then, if.exit53)
  select.phi172 = phi [bb %then, i1 %select.phi168, bb %if.exit53, i1 %204]
  br bb %if.exit
then52:  pred(then37)
  br i1 %32, bb %while.exit26, bb %if.exit53
if.exit53:  pred(then37, then52)
  br bb %if.exit38
while.body64.iter1:  pred(if.exit)
  i.phi132244354 = phi [bb %if.exit, i32 %i.phi124]
  j.phi145243355 = phi [bb %if.exit, i32 %j.phi137]
  k.phi158242356 = phi [bb %if.exit, i32 %k.phi148]
  82 = add i32 %sum.phi, i32 1
  br bb %while.cond24
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


