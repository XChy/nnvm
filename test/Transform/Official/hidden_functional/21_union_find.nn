internal global ptr parent init with 1005 x i32 zeros
external i32 getint()

external void putint(i32 )

i32 main() {
entry46:  pred()
  parent.pinned = pin ptr @parent
  48 = call i32 getint()
  50 = call i32 getint()
  60167 = icmp slt i32 0, i32 %48
  br i1 %60167, bb %while.body, bb %while.cond71_preheader
while.body:  pred(while.body, entry46)
  i.phi117168 = phi [bb %while.body, i32 %69, bb %entry46, i32 0]
  148 = shl i32 %i.phi117168, i32 2
  64 = ptradd ptr %parent.pinned, i32 %148
  store i32 %i.phi117168 to ptr %64
  69 = add i32 %i.phi117168, i32 1
  60 = icmp slt i32 %69, i32 %48
  br i1 %60, bb %while.body, bb %while.cond71_preheader
while.cond71_preheader:  pred(while.body, entry46)
  76165 = icmp slt i32 0, i32 %50
  br i1 %76165, bb %while.body72, bb %while.cond90_preheader
while.body72:  pred(merge.if.exit40, while.cond71_preheader)
  i.phi116166 = phi [bb %merge.if.exit40, i32 %87, bb %while.cond71_preheader, i32 0]
  79 = call i32 getint()
  81 = call i32 getint()
  32133 = call i32 find(i32 %79)
  35134 = call i32 find(i32 %81)
  38135 = icmp ne i32 %32133, i32 %35134
  br i1 %38135, bb %merge.then39, bb %merge.if.exit40
while.cond90_preheader:  pred(merge.if.exit40, while.cond71_preheader)
  br i1 %60167, bb %while.body91, bb %while.exit92
while.body91:  pred(while.body91.split, while.cond90_preheader)
  i.phi164 = phi [bb %while.body91.split, i32 %110, bb %while.cond90_preheader, i32 0]
  clusters.phi121163 = phi [bb %while.body91.split, i32 %198, bb %while.cond90_preheader, i32 0]
  149 = shl i32 %i.phi164, i32 2
  99 = ptradd ptr %parent.pinned, i32 %149
  99.load = load i32 from ptr %99
  102 = icmp eq i32 %99.load, i32 %i.phi164
  107 = add i32 %clusters.phi121163, i32 1
  br i1 %102, bb %clusters.phi.which.true, bb %while.body91.split
while.body91.split:  pred(while.body91, clusters.phi.which.true)
  198 = phi [bb %clusters.phi.which.true, i32 %107, bb %while.body91, i32 %clusters.phi121163]
  110 = add i32 %i.phi164, i32 1
  95 = icmp slt i32 %110, i32 %48
  br i1 %95, bb %while.body91, bb %while.exit92
while.exit92:  pred(while.body91.split, while.cond90_preheader)
  clusters.phi121_ph_val162 = phi [bb %while.body91.split, i32 %198, bb %while.cond90_preheader, i32 0]
  call void putint(i32 %clusters.phi121_ph_val162)
  ret i32 0
merge.then39:  pred(while.body72)
  150 = shl i32 %35134, i32 2
  43139 = ptradd ptr %parent.pinned, i32 %150
  store i32 %32133 to ptr %43139
  br bb %merge.if.exit40
merge.if.exit40:  pred(while.body72, merge.then39)
  87 = add i32 %i.phi116166, i32 1
  76 = icmp slt i32 %87, i32 %50
  br i1 %76, bb %while.body72, bb %while.cond90_preheader
clusters.phi.which.true:  pred(while.body91)
  br bb %while.body91.split
}

internal i32 find(i32 root) {
entry:  pred()
  151 = shl i32 %root, i32 2
  4 = ptradd ptr @parent, i32 %151
  4.load = load i32 from ptr %4
  7 = icmp eq i32 %4.load, i32 %root
  br i1 %7, bb %then, bb %else
then:  pred(entry)
  ret i32 %root
else:  pred(entry)
  18.load = load i32 from ptr %4
  20 = call i32 find(i32 %18.load)
  store i32 %20 to ptr %4
  ret i32 %20
}


