internal global ptr array init with 1000 x i32 zeros
internal void findSmallest(i32 low, i32 high, i32 k, i32 n) {
entry61:  pred()
  68 = icmp eq i32 %low, i32 %high
  br i1 %68, bb %then69, bb %else
then69:  pred(entry61)
  ret 
else:  pred(entry61)
  array.pinned = pin ptr @array
  243 = shl i32 %high, i32 2
  27167 = ptradd ptr %array.pinned, i32 %243
  27.load168 = load i32 from ptr %27167
  38173251 = icmp slt i32 %low, i32 %high
  247252 = shl i32 %low, i32 2
  53186253 = add i32 %low, i32 1
  br i1 %38173251, bb %findPivot.while.body_preheader, bb %findPivot.while.exit
then79:  pred(findPivot.while.exit)
  88249 = icmp slt i32 0, i32 %low254
  br i1 %88249, bb %while.body84_preheader, bb %if.exit80
if.exit80:  pred(then103, else105, then79, while.body84)
  ret 
else81:  pred(findPivot.while.exit)
  102 = icmp slt i32 %k, i32 %low254
  br i1 %102, bb %then103, bb %else105
while.body84_preheader:  pred(then79)
  br bb %while.body84
while.body84:  pred(while.body84, while.body84_preheader)
  i82.phi250 = phi [bb %while.body84, i32 %99, bb %while.body84_preheader, i32 0]
  244 = shl i32 %i82.phi250, i32 2
  92 = ptradd ptr %array.pinned, i32 %244
  92.load = load i32 from ptr %92
  call void putint(i32 %92.load)
  call void putch(i32 32)
  99 = add i32 %i82.phi250, i32 1
  88 = icmp slt i32 %99, i32 %low254
  br i1 %88, bb %while.body84, bb %if.exit80
then103:  pred(else81)
  155 = add i32 %low254, i32 -1
  call void findSmallest(i32 %low, i32 %155, i32 %k, i32 %n)
  br bb %if.exit80
else105:  pred(else81)
  call void findSmallest(i32 %53186253256, i32 %high, i32 %k, i32 %n)
  br bb %if.exit80
findPivot.while.body_preheader:  pred(else)
  br bb %findPivot.while.body
findPivot.while.body:  pred(findPivot.if.exit, findPivot.while.body_preheader)
  53186253260 = phi [bb %findPivot.if.exit, i32 %53186, bb %findPivot.while.body_preheader, i32 %53186253]
  247252259 = phi [bb %findPivot.if.exit, i32 %247, bb %findPivot.while.body_preheader, i32 %247252]
  pIndex.phi151172258 = phi [bb %findPivot.if.exit, i32 %pIndex.phi189, bb %findPivot.while.body_preheader, i32 %low]
  i.phi171257 = phi [bb %findPivot.if.exit, i32 %56190, bb %findPivot.while.body_preheader, i32 %low]
  245 = shl i32 %i.phi171257, i32 2
  42177 = ptradd ptr %array.pinned, i32 %245
  42.load178 = load i32 from ptr %42177
  45179 = icmp sle i32 %42.load178, i32 %27.load168
  br i1 %45179, bb %findPivot.then, bb %findPivot.if.exit
findPivot.while.exit:  pred(else, findPivot.while.body_exclu_exit)
  53186253256 = phi [bb %findPivot.while.body_exclu_exit, i32 %53186, bb %else, i32 %53186253]
  247252255 = phi [bb %findPivot.while.body_exclu_exit, i32 %247, bb %else, i32 %247252]
  low254 = phi [bb %findPivot.while.body_exclu_exit, i32 %pIndex.phi189, bb %else, i32 %low]
  6218 = ptradd ptr %array.pinned, i32 %247252255
  6.load219 = load i32 from ptr %6218
  14.load222 = load i32 from ptr %27167
  store i32 %14.load222 to ptr %6218
  store i32 %6.load219 to ptr %27167
  78 = icmp eq i32 %k, i32 %low254
  br i1 %78, bb %then79, bb %else81
findPivot.then:  pred(findPivot.while.body)
  6.load230 = load i32 from ptr %42177
  14232 = ptradd ptr %array.pinned, i32 %247252259
  14.load233 = load i32 from ptr %14232
  store i32 %14.load233 to ptr %42177
  store i32 %6.load230 to ptr %14232
  br bb %findPivot.if.exit
findPivot.if.exit:  pred(findPivot.while.body, findPivot.then)
  pIndex.phi189 = phi [bb %findPivot.while.body, i32 %pIndex.phi151172258, bb %findPivot.then, i32 %53186253260]
  56190 = add i32 %i.phi171257, i32 1
  38173 = icmp slt i32 %56190, i32 %high
  247 = shl i32 %pIndex.phi189, i32 2
  53186 = add i32 %pIndex.phi189, i32 1
  br i1 %38173, bb %findPivot.while.body, bb %findPivot.while.body_exclu_exit
findPivot.while.body_exclu_exit:  pred(findPivot.if.exit)
  br bb %findPivot.while.exit
}

external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry118:  pred()
  120 = call i32 getint()
  122 = call i32 getint()
  129261 = icmp slt i32 0, i32 %120
  br i1 %129261, bb %while.body125_preheader, bb %while.exit126
while.body125_preheader:  pred(entry118)
  br bb %while.body125
while.body125:  pred(while.body125, while.body125_preheader)
  i123.phi262 = phi [bb %while.body125, i32 %138, bb %while.body125_preheader, i32 0]
  248 = shl i32 %i123.phi262, i32 2
  133 = ptradd ptr @array, i32 %248
  135 = call i32 getint()
  store i32 %135 to ptr %133
  138 = add i32 %i123.phi262, i32 1
  129 = icmp slt i32 %138, i32 %120
  br i1 %129, bb %while.body125, bb %while.exit126
while.exit126:  pred(entry118, while.body125)
  159 = add i32 %120, i32 -1
  call void findSmallest(i32 0, i32 %159, i32 %122, i32 %120)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


