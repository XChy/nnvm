internal global ptr array init with 1000 x i32 zeros
internal void findSmallest(i32 low, i32 high, i32 k, i32 n) {
entry61:  pred()
  68 = icmp eq i32 %low, i32 %high
  br i1 %68, bb %then69, bb %else
then69:  pred(entry61)
  ret 
else:  pred(entry61)
  array.pinned = pin ptr @array
  247 = shl i32 %high, i32 2
  27171 = ptradd ptr %array.pinned, i32 %247
  27.load172 = load i32 from ptr %27171
  38177255 = icmp slt i32 %low, i32 %high
  251256 = shl i32 %low, i32 2
  53190257 = add i32 %low, i32 1
  br i1 %38177255, bb %findPivot.while.body_preheader, bb %findPivot.while.exit
then79:  pred(findPivot.while.exit)
  88253 = icmp slt i32 0, i32 %low258
  br i1 %88253, bb %while.body84_preheader, bb %if.exit80
if.exit80:  pred(then103, else105, then79, while.body84)
  ret 
else81:  pred(findPivot.while.exit)
  102 = icmp slt i32 %k, i32 %low258
  br i1 %102, bb %then103, bb %else105
while.body84_preheader:  pred(then79)
  br bb %while.body84
while.body84:  pred(while.body84, while.body84_preheader)
  i82.phi254 = phi [bb %while.body84, i32 %99, bb %while.body84_preheader, i32 0]
  248 = shl i32 %i82.phi254, i32 2
  92 = ptradd ptr %array.pinned, i32 %248
  92.load = load i32 from ptr %92
  call void putint(i32 %92.load)
  call void putch(i32 32)
  99 = add i32 %i82.phi254, i32 1
  88 = icmp slt i32 %99, i32 %low258
  br i1 %88, bb %while.body84, bb %if.exit80
then103:  pred(else81)
  159 = add i32 %low258, i32 -1
  call void findSmallest(i32 %low, i32 %159, i32 %k, i32 %n)
  br bb %if.exit80
else105:  pred(else81)
  call void findSmallest(i32 %53190257260, i32 %high, i32 %k, i32 %n)
  br bb %if.exit80
findPivot.while.body_preheader:  pred(else)
  br bb %findPivot.while.body
findPivot.while.body:  pred(findPivot.if.exit, findPivot.while.body_preheader)
  53190257264 = phi [bb %findPivot.if.exit, i32 %53190, bb %findPivot.while.body_preheader, i32 %53190257]
  251256263 = phi [bb %findPivot.if.exit, i32 %251, bb %findPivot.while.body_preheader, i32 %251256]
  pIndex.phi153176262 = phi [bb %findPivot.if.exit, i32 %pIndex.phi193, bb %findPivot.while.body_preheader, i32 %low]
  i.phi175261 = phi [bb %findPivot.if.exit, i32 %56194, bb %findPivot.while.body_preheader, i32 %low]
  249 = shl i32 %i.phi175261, i32 2
  42181 = ptradd ptr %array.pinned, i32 %249
  42.load182 = load i32 from ptr %42181
  45183 = icmp sle i32 %42.load182, i32 %27.load172
  br i1 %45183, bb %findPivot.then, bb %findPivot.if.exit
findPivot.while.exit:  pred(else, findPivot.while.body_exclu_exit)
  53190257260 = phi [bb %findPivot.while.body_exclu_exit, i32 %53190, bb %else, i32 %53190257]
  251256259 = phi [bb %findPivot.while.body_exclu_exit, i32 %251, bb %else, i32 %251256]
  low258 = phi [bb %findPivot.while.body_exclu_exit, i32 %pIndex.phi193, bb %else, i32 %low]
  6222 = ptradd ptr %array.pinned, i32 %251256259
  6.load223 = load i32 from ptr %6222
  14.load226 = load i32 from ptr %27171
  store i32 %14.load226 to ptr %6222
  store i32 %6.load223 to ptr %27171
  78 = icmp eq i32 %k, i32 %low258
  br i1 %78, bb %then79, bb %else81
findPivot.then:  pred(findPivot.while.body)
  6.load234 = load i32 from ptr %42181
  14236 = ptradd ptr %array.pinned, i32 %251256263
  14.load237 = load i32 from ptr %14236
  store i32 %14.load237 to ptr %42181
  store i32 %6.load234 to ptr %14236
  br bb %findPivot.if.exit
findPivot.if.exit:  pred(findPivot.while.body, findPivot.then)
  pIndex.phi193 = phi [bb %findPivot.while.body, i32 %pIndex.phi153176262, bb %findPivot.then, i32 %53190257264]
  56194 = add i32 %i.phi175261, i32 1
  38177 = icmp slt i32 %56194, i32 %high
  251 = shl i32 %pIndex.phi193, i32 2
  53190 = add i32 %pIndex.phi193, i32 1
  br i1 %38177, bb %findPivot.while.body, bb %findPivot.while.body_exclu_exit
findPivot.while.body_exclu_exit:  pred(findPivot.if.exit)
  br bb %findPivot.while.exit
}

external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry118:  pred()
  120 = call i32 getint()
  122 = call i32 getint()
  129265 = icmp slt i32 0, i32 %120
  br i1 %129265, bb %while.body125_preheader, bb %while.exit126
while.body125_preheader:  pred(entry118)
  br bb %while.body125
while.body125:  pred(while.body125, while.body125_preheader)
  i123.phi266 = phi [bb %while.body125, i32 %138, bb %while.body125_preheader, i32 0]
  252 = shl i32 %i123.phi266, i32 2
  133 = ptradd ptr @array, i32 %252
  135 = call i32 getint()
  store i32 %135 to ptr %133
  138 = add i32 %i123.phi266, i32 1
  129 = icmp slt i32 %138, i32 %120
  br i1 %129, bb %while.body125, bb %while.exit126
while.exit126:  pred(entry118, while.body125)
  163 = add i32 %120, i32 -1
  call void findSmallest(i32 0, i32 %163, i32 %122, i32 %120)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


