internal global ptr array init with 1000 x i32 zeros
immutable internal global ptr space init with i32 32
immutable internal global ptr maxN init with i32 1000
void findSmallest(i32 low, i32 high, i32 k, i32 n) {
entry61:  pred()
  68 = icmp eq i32 %low, i32 %high
  br i1 %68, bb %then69, bb %else
then69:  pred(entry61)
  ret 
else:  pred(entry61)
  155164 = shl i32 %high, i32 2
  27165 = ptradd ptr @array, i32 %155164
  27.load166 = load i32 from ptr %27165
  38171237 = icmp slt i32 %low, i32 %high
  br i1 %38171237, bb %findPivot.while.body_preheader, bb %findPivot.while.exit
then79:  pred(findPivot.while.exit)
  88235 = icmp slt i32 0, i32 %low238
  br i1 %88235, bb %while.body84_preheader, bb %if.exit80
if.exit80:  pred(while.body84, then103, else105, then79)
  ret 
else81:  pred(findPivot.while.exit)
  102 = icmp slt i32 %k, i32 %low238
  br i1 %102, bb %then103, bb %else105
while.body84_preheader:  pred(then79)
  br bb %while.body84
while.body84:  pred(while.body84, while.body84_preheader)
  i82.phi236 = phi [bb %while.body84, i32 %99, bb %while.body84_preheader, i32 0]
  154 = shl i32 %i82.phi236, i32 2
  92 = ptradd ptr @array, i32 %154
  92.load = load i32 from ptr %92
  call void putint(i32 %92.load)
  call void putch(i32 32)
  99 = add i32 %i82.phi236, i32 1
  88 = icmp slt i32 %99, i32 %low238
  br i1 %88, bb %while.body84, bb %if.exit80
then103:  pred(else81)
  108 = sub i32 %low238, i32 1
  call void findSmallest(i32 %low, i32 %108, i32 %k, i32 %n)
  br bb %if.exit80
else105:  pred(else81)
  113 = add i32 %low238, i32 1
  call void findSmallest(i32 %113, i32 %high, i32 %k, i32 %n)
  br bb %if.exit80
findPivot.while.body_preheader:  pred(else)
  br bb %findPivot.while.body
findPivot.while.body:  pred(findPivot.if.exit, findPivot.while.body_preheader)
  pIndex.phi151170240 = phi [bb %findPivot.if.exit, i32 %pIndex.phi187, bb %findPivot.while.body_preheader, i32 %low]
  i.phi169239 = phi [bb %findPivot.if.exit, i32 %56188, bb %findPivot.while.body_preheader, i32 %low]
  156174 = shl i32 %i.phi169239, i32 2
  42175 = ptradd ptr @array, i32 %156174
  42.load176 = load i32 from ptr %42175
  45177 = icmp sle i32 %42.load176, i32 %27.load166
  br i1 %45177, bb %findPivot.then, bb %findPivot.if.exit
findPivot.while.exit:  pred(findPivot.if.exit, else)
  low238 = phi [bb %findPivot.if.exit, i32 %pIndex.phi187, bb %else, i32 %low]
  158215 = shl i32 %low238, i32 2
  6216 = ptradd ptr @array, i32 %158215
  6.load217 = load i32 from ptr %6216
  14.load220 = load i32 from ptr %27165
  store i32 %14.load220 to ptr %6216
  store i32 %6.load217 to ptr %27165
  78 = icmp eq i32 %k, i32 %low238
  br i1 %78, bb %then79, bb %else81
findPivot.then:  pred(findPivot.while.body)
  6.load228 = load i32 from ptr %42175
  160229 = shl i32 %pIndex.phi151170240, i32 2
  14230 = ptradd ptr @array, i32 %160229
  14.load231 = load i32 from ptr %14230
  store i32 %14.load231 to ptr %42175
  store i32 %6.load228 to ptr %14230
  53184 = add i32 %pIndex.phi151170240, i32 1
  br bb %findPivot.if.exit
findPivot.if.exit:  pred(findPivot.while.body, findPivot.then)
  pIndex.phi187 = phi [bb %findPivot.while.body, i32 %pIndex.phi151170240, bb %findPivot.then, i32 %53184]
  56188 = add i32 %i.phi169239, i32 1
  38171 = icmp slt i32 %56188, i32 %high
  br i1 %38171, bb %findPivot.while.body, bb %findPivot.while.exit
}

i32 findPivot(i32 start, i32 end) {
entry21:  pred()
  155 = shl i32 %end, i32 2
  27 = ptradd ptr @array, i32 %155
  27.load = load i32 from ptr %27
  38241 = icmp slt i32 %start, i32 %end
  br i1 %38241, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry21)
  br bb %while.body
while.body:  pred(if.exit, while.body_preheader)
  pIndex.phi151244 = phi [bb %if.exit, i32 %pIndex.phi, bb %while.body_preheader, i32 %start]
  i.phi243 = phi [bb %if.exit, i32 %56, bb %while.body_preheader, i32 %start]
  156 = shl i32 %i.phi243, i32 2
  42 = ptradd ptr @array, i32 %156
  42.load = load i32 from ptr %42
  45 = icmp sle i32 %42.load, i32 %27.load
  br i1 %45, bb %then, bb %if.exit
while.exit:  pred(if.exit, entry21)
  start = phi [bb %if.exit, i32 %pIndex.phi, bb %entry21, i32 %start]
  158204 = shl i32 %start, i32 2
  6205 = ptradd ptr @array, i32 %158204
  6.load206 = load i32 from ptr %6205
  14.load209 = load i32 from ptr %27
  store i32 %14.load209 to ptr %6205
  store i32 %6.load206 to ptr %27
  ret i32 %start
then:  pred(while.body)
  6.load195 = load i32 from ptr %42
  160196 = shl i32 %pIndex.phi151244, i32 2
  14197 = ptradd ptr @array, i32 %160196
  14.load198 = load i32 from ptr %14197
  store i32 %14.load198 to ptr %42
  store i32 %6.load195 to ptr %14197
  53 = add i32 %pIndex.phi151244, i32 1
  br bb %if.exit
if.exit:  pred(while.body, then)
  pIndex.phi = phi [bb %while.body, i32 %pIndex.phi151244, bb %then, i32 %53]
  56 = add i32 %i.phi243, i32 1
  38 = icmp slt i32 %56, i32 %end
  br i1 %38, bb %while.body, bb %while.exit
}

external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry118:  pred()
  120 = call i32 getint()
  122 = call i32 getint()
  129245 = icmp slt i32 0, i32 %120
  br i1 %129245, bb %while.body125_preheader, bb %while.exit126
while.body125_preheader:  pred(entry118)
  br bb %while.body125
while.body125:  pred(while.body125, while.body125_preheader)
  i123.phi246 = phi [bb %while.body125, i32 %138, bb %while.body125_preheader, i32 0]
  157 = shl i32 %i123.phi246, i32 2
  133 = ptradd ptr @array, i32 %157
  135 = call i32 getint()
  store i32 %135 to ptr %133
  138 = add i32 %i123.phi246, i32 1
  129 = icmp slt i32 %138, i32 %120
  br i1 %129, bb %while.body125, bb %while.exit126
while.exit126:  pred(while.body125, entry118)
  142 = sub i32 %120, i32 1
  call void findSmallest(i32 0, i32 %142, i32 %122, i32 %120)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )

void swap(i32 i, i32 j) {
entry:  pred()
  158 = shl i32 %i, i32 2
  6 = ptradd ptr @array, i32 %158
  6.load = load i32 from ptr %6
  160 = shl i32 %j, i32 2
  14 = ptradd ptr @array, i32 %160
  14.load = load i32 from ptr %14
  store i32 %14.load to ptr %6
  store i32 %6.load to ptr %14
  ret 
}


