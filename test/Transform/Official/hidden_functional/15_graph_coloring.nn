immutable global ptr LF init with i32 10
immutable global ptr space init with i32 32
immutable global ptr V init with i32 4
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

i32 graphColoring(ptr graph, i32 m, i32 i, ptr color) {
entry78:  pred()
  83 = icmp eq i32 %i, i32 4
  br i1 %83, bb %then84, bb %if.exit85
then84:  pred(entry78)
  br bb %isSafe.while.cond33
then84.split:  pred(isSafe.while.exit35, isSafe.then)
  86.ret = phi [bb %isSafe.while.exit35, i32 1, bb %isSafe.then, i32 0]
  87 = icmp ne i32 %86.ret, i32 0
  br i1 %87, bb %then88, bb %if.exit89
if.exit85:  pred(entry78)
  br bb %while.cond92
then88:  pred(then84.split)
  7238251 = icmp slt i32 0, i32 4
  br i1 %7238251, bb %printSolution.while.body, bb %printSolution.while.exit
if.exit89:  pred(then84.split)
  ret i32 0
while.cond92:  pred(if.exit85, if.exit109)
  j91.phi = phi [bb %if.exit85, i32 1, bb %if.exit109, i32 %114]
  97 = icmp sle i32 %j91.phi, i32 %m
  br i1 %97, bb %while.body93, bb %while.exit94
while.body93:  pred(while.cond92)
  166 = shl i32 %i, i32 2
  101 = ptradd ptr %color, i32 %166
  store i32 %j91.phi to ptr %101
  105 = add i32 %i, i32 1
  106 = call i32 graphColoring(ptr %graph, i32 %m, i32 %105, ptr %color)
  107 = icmp ne i32 %106, i32 0
  br i1 %107, bb %then108, bb %if.exit109
while.exit94:  pred(while.cond92)
  ret i32 0
then108:  pred(while.body93)
  ret i32 1
if.exit109:  pred(while.body93)
  store i32 0 to ptr %101
  114 = add i32 %j91.phi, i32 1
  br bb %while.cond92
isSafe.while.cond33:  pred(then84, isSafe.while.exit45)
  select.phi164178 = phi [bb %then84, i1 0, bb %isSafe.while.exit45, i1 %select.phi163188]
  i32.phi179 = phi [bb %then84, i32 0, bb %isSafe.while.exit45, i32 %42183]
  38180 = icmp slt i32 %i32.phi179, i32 4
  br i1 %38180, bb %isSafe.while.body34, bb %isSafe.while.exit35
isSafe.while.body34:  pred(isSafe.while.cond33)
  42183 = add i32 %i32.phi179, i32 1
  br bb %isSafe.while.cond43
isSafe.while.exit35:  pred(isSafe.while.cond33)
  br bb %then84.split
isSafe.while.cond43:  pred(isSafe.while.body34, isSafe.if.exit)
  select.phi163188 = phi [bb %isSafe.while.body34, i1 %select.phi164178, bb %isSafe.if.exit, i1 %select.phi213]
  j.phi189 = phi [bb %isSafe.while.body34, i32 %42183, bb %isSafe.if.exit, i32 %75218]
  48190 = icmp slt i32 %j.phi189, i32 4
  br i1 %48190, bb %isSafe.while.body44, bb %isSafe.while.exit45
isSafe.while.body44:  pred(isSafe.while.cond43)
  169193 = shl i32 %i32.phi179, i32 4
  52194 = ptradd ptr %graph, i32 %169193
  170195 = shl i32 %j.phi189, i32 2
  55196 = ptradd ptr %52194, i32 %170195
  55.load197 = load i32 from ptr %55196
  57198 = icmp ne i32 %55.load197, i32 0
  br i1 %57198, bb %isSafe.select.then, bb %isSafe.select.else
isSafe.while.exit45:  pred(isSafe.while.cond43)
  br bb %isSafe.while.cond33
isSafe.select.then:  pred(isSafe.while.body44)
  64203 = ptradd ptr %color, i32 %170195
  64.load204 = load i32 from ptr %64203
  172205 = shl i32 %i32.phi179, i32 2
  68206 = ptradd ptr %color, i32 %172205
  68.load207 = load i32 from ptr %68206
  70208 = icmp eq i32 %64.load204, i32 %68.load207
  br bb %isSafe.select.exit
isSafe.select.else:  pred(isSafe.while.body44)
  br bb %isSafe.select.exit
isSafe.select.exit:  pred(isSafe.select.then, isSafe.select.else)
  select.phi213 = phi [bb %isSafe.select.else, i1 0, bb %isSafe.select.then, i1 %70208]
  br i1 %select.phi213, bb %isSafe.then, bb %isSafe.if.exit
isSafe.then:  pred(isSafe.select.exit)
  br bb %then84.split
isSafe.if.exit:  pred(isSafe.select.exit)
  75218 = add i32 %j.phi189, i32 1
  br bb %isSafe.while.cond43
printSolution.while.cond:  pred(printSolution.while.body)
  i.phi237 = phi [bb %printSolution.while.body, i32 %17246]
  7238 = icmp slt i32 %i.phi237, i32 4
  br i1 %7238, bb %printSolution.while.body, bb %printSolution.while.exit
printSolution.while.body:  pred(printSolution.while.cond, then88)
  i.phi237252 = phi [bb %then88, i32 0, bb %printSolution.while.cond, i32 %i.phi237]
  173241 = shl i32 %i.phi237252, i32 2
  11242 = ptradd ptr %color, i32 %173241
  11.load243 = load i32 from ptr %11242
  call void putint(i32 %11.load243)
  call void putch(i32 32)
  17246 = add i32 %i.phi237252, i32 1
  br bb %printSolution.while.cond
printSolution.while.exit:  pred(printSolution.while.cond, then88)
  call void putch(i32 10)
  ret i32 1
}

external i32 getfarray(ptr )

i32 main() {
entry115:  pred()
  %color = stack 16
  %graph = stack 64
  store i32 0 to ptr %graph
  arr.index118 = ptradd ptr %graph, i32 4
  store i32 1 to ptr %arr.index118
  arr.index119 = ptradd ptr %graph, i32 8
  store i32 1 to ptr %arr.index119
  arr.index120 = ptradd ptr %graph, i32 12
  store i32 1 to ptr %arr.index120
  arr.index121 = ptradd ptr %graph, i32 16
  store i32 1 to ptr %arr.index121
  arr.index122 = ptradd ptr %graph, i32 20
  store i32 0 to ptr %arr.index122
  arr.index123 = ptradd ptr %graph, i32 24
  store i32 1 to ptr %arr.index123
  arr.index124 = ptradd ptr %graph, i32 28
  store i32 0 to ptr %arr.index124
  arr.index125 = ptradd ptr %graph, i32 32
  store i32 1 to ptr %arr.index125
  arr.index126 = ptradd ptr %graph, i32 36
  store i32 1 to ptr %arr.index126
  arr.index127 = ptradd ptr %graph, i32 40
  store i32 0 to ptr %arr.index127
  arr.index128 = ptradd ptr %graph, i32 44
  store i32 1 to ptr %arr.index128
  arr.index129 = ptradd ptr %graph, i32 48
  store i32 1 to ptr %arr.index129
  arr.index130 = ptradd ptr %graph, i32 52
  store i32 0 to ptr %arr.index130
  arr.index131 = ptradd ptr %graph, i32 56
  store i32 1 to ptr %arr.index131
  arr.index132 = ptradd ptr %graph, i32 60
  store i32 0 to ptr %arr.index132
  141253 = icmp slt i32 0, i32 4
  br i1 %141253, bb %while.body137, bb %while.exit138
while.cond136:  pred(while.body137)
  i135.phi = phi [bb %while.body137, i32 %147]
  141 = icmp slt i32 %i135.phi, i32 4
  br i1 %141, bb %while.body137, bb %while.exit138
while.body137:  pred(while.cond136, entry115)
  i135.phi254 = phi [bb %entry115, i32 0, bb %while.cond136, i32 %i135.phi]
  168 = shl i32 %i135.phi254, i32 2
  145 = ptradd ptr %color, i32 %168
  store i32 0 to ptr %145
  147 = add i32 %i135.phi254, i32 1
  br bb %while.cond136
while.exit138:  pred(while.cond136, entry115)
  149 = call i32 graphColoring(ptr %graph, i32 3, i32 0, ptr %color)
  150 = icmp ne i32 %149, i32 0
  151 = xor i1 %150, i1 -1
  152 = zext i1 %151 to i32
  153 = icmp ne i32 %152, i32 0
  br i1 %153, bb %then154, bb %if.exit155
then154:  pred(while.exit138)
  call void putch(i32 78)
  call void putch(i32 111)
  call void putch(i32 116)
  call void putch(i32 32)
  call void putch(i32 101)
  call void putch(i32 120)
  call void putch(i32 105)
  call void putch(i32 115)
  call void putch(i32 116)
  br bb %if.exit155
if.exit155:  pred(while.exit138, then154)
  ret i32 0
}

i32 isSafe(ptr graph, ptr color) {
entry31:  pred()
  br bb %while.cond33
while.cond33:  pred(entry31, while.exit45)
  select.phi164 = phi [bb %entry31, i1 0, bb %while.exit45, i1 %select.phi163]
  i32.phi = phi [bb %entry31, i32 0, bb %while.exit45, i32 %42]
  38 = icmp slt i32 %i32.phi, i32 4
  br i1 %38, bb %while.body34, bb %while.exit35
while.body34:  pred(while.cond33)
  42 = add i32 %i32.phi, i32 1
  br bb %while.cond43
while.exit35:  pred(while.cond33)
  ret i32 1
while.cond43:  pred(while.body34, if.exit)
  select.phi163 = phi [bb %while.body34, i1 %select.phi164, bb %if.exit, i1 %select.phi]
  j.phi = phi [bb %while.body34, i32 %42, bb %if.exit, i32 %75]
  48 = icmp slt i32 %j.phi, i32 4
  br i1 %48, bb %while.body44, bb %while.exit45
while.body44:  pred(while.cond43)
  169 = shl i32 %i32.phi, i32 4
  52 = ptradd ptr %graph, i32 %169
  170 = shl i32 %j.phi, i32 2
  55 = ptradd ptr %52, i32 %170
  55.load = load i32 from ptr %55
  57 = icmp ne i32 %55.load, i32 0
  br i1 %57, bb %select.then, bb %select.else
while.exit45:  pred(while.cond43)
  br bb %while.cond33
select.then:  pred(while.body44)
  64 = ptradd ptr %color, i32 %170
  64.load = load i32 from ptr %64
  172 = shl i32 %i32.phi, i32 2
  68 = ptradd ptr %color, i32 %172
  68.load = load i32 from ptr %68
  70 = icmp eq i32 %64.load, i32 %68.load
  br bb %select.exit
select.else:  pred(while.body44)
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 0, bb %select.then, i1 %70]
  br i1 %select.phi, bb %then, bb %if.exit
then:  pred(select.exit)
  ret i32 0
if.exit:  pred(select.exit)
  75 = add i32 %j.phi, i32 1
  br bb %while.cond43
}

void printMessage() {
entry20:  pred()
  call void putch(i32 78)
  call void putch(i32 111)
  call void putch(i32 116)
  call void putch(i32 32)
  call void putch(i32 101)
  call void putch(i32 120)
  call void putch(i32 105)
  call void putch(i32 115)
  call void putch(i32 116)
  ret 
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void _sysy_stoptime(i32 )

void printSolution(ptr color) {
entry:  pred()
  7255 = icmp slt i32 0, i32 4
  br i1 %7255, bb %while.body, bb %while.exit
while.cond:  pred(while.body)
  i.phi = phi [bb %while.body, i32 %17]
  7 = icmp slt i32 %i.phi, i32 4
  br i1 %7, bb %while.body, bb %while.exit
while.body:  pred(while.cond, entry)
  i.phi256 = phi [bb %entry, i32 0, bb %while.cond, i32 %i.phi]
  173 = shl i32 %i.phi256, i32 2
  11 = ptradd ptr %color, i32 %173
  11.load = load i32 from ptr %11
  call void putint(i32 %11.load)
  call void putch(i32 32)
  17 = add i32 %i.phi256, i32 1
  br bb %while.cond
while.exit:  pred(while.cond, entry)
  call void putch(i32 10)
  ret 
}

external void putfarray(i32 , ptr )


