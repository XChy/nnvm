global ptr lim init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry34:  pred()
  36 = call i32 getint()
  store i32 %36 to ptr @lim
  call void _sysy_starttime(i32 22)
  lim.load4498 = load i32 from ptr @lim
  4599 = icmp sle i32 1, i32 %lim.load4498
  br i1 %4599, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry34)
  br bb %while.body
while.body:  pred(while.body.split, while.body_preheader)
  ans.phi102 = phi [bb %while.body.split, i32 %53, bb %while.body_preheader, i32 0]
  i.phi101 = phi [bb %while.body.split, i32 %56, bb %while.body_preheader, i32 1]
  br bb %fun.loop_entry
while.body.split:  pred(fun.then, fun.else27)
  50.ret = phi [bb %fun.then, i32 %dep.loop72, bb %fun.else27, i32 0]
  51 = add i32 %ans.phi102, i32 %50.ret
  53 = srem i32 %51, i32 1000000007
  56 = add i32 %i.phi101, i32 1
  45 = icmp sle i32 %56, i32 %lim.load4498
  br i1 %45, bb %while.body, bb %while.body_exclu_exit
while.exit:  pred(entry34, while.body_exclu_exit)
  100 = phi [bb %while.body_exclu_exit, i32 %53, bb %entry34, i32 0]
  call void _sysy_stoptime(i32 27)
  call void putint(i32 %100)
  call void putch(i32 10)
  ret i32 0
fun.loop_entry:  pred(while.body, fun.loop_entry_unified_latch)
  dep.loop72 = phi [bb %while.body, i32 0, bb %fun.loop_entry_unified_latch, i32 %1884]
  n.loop73 = phi [bb %while.body, i32 %i.phi101, bb %fun.loop_entry_unified_latch, i32 %106]
  474 = icmp eq i32 %n.loop73, i32 1
  br i1 %474, bb %fun.then, bb %fun.else
fun.then:  pred(fun.loop_entry)
  br bb %while.body.split
fun.else:  pred(fun.loop_entry)
  6379 = and i32 %n.loop73, i32 1
  6480 = icmp eq i32 %6379, i32 0
  1884 = add i32 %dep.loop72, i32 1
  br i1 %6480, bb %fun.then12, bb %fun.else14
fun.then12:  pred(fun.else)
  1683 = sdiv i32 %n.loop73, i32 2
  br bb %fun.loop_entry_unified_latch
fun.else14:  pred(fun.else)
  2187 = mul i32 %n.loop73, i32 3
  2288 = add i32 %2187, i32 1
  2490 = icmp sle i32 %2288, i32 %lim.load4498
  br i1 %2490, bb %fun.then25, bb %fun.else27
fun.then25:  pred(fun.else14)
  br bb %fun.loop_entry_unified_latch
fun.else27:  pred(fun.else14)
  br bb %while.body.split
fun.loop_entry_unified_latch:  pred(fun.then12, fun.then25)
  106 = phi [bb %fun.then12, i32 %1683, bb %fun.then25, i32 %2288]
  br bb %fun.loop_entry
while.body_exclu_exit:  pred(while.body.split)
  br bb %while.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


