global ptr COUNT init with i32 500000
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getfarray(ptr )

i32 main() {
entry27:  pred()
  %y = stack 19600
  %x = stack 19600
  call void _sysy_starttime(i32 22)
  COUNT.load158 = load i32 from ptr @COUNT
  42159 = icmp slt i32 0, i32 %COUNT.load158
  br i1 %42159, bb %while.body38_preheader, bb %while.exit39
while.body38_preheader:  pred(entry27)
  br bb %while.body38
while.body38:  pred(loop.while.exit, while.body38_preheader)
  i28.phi165 = phi [bb %loop.while.exit, i32 %91, bb %while.body38_preheader, i32 0]
  j.phi116164 = phi [bb %loop.while.exit, i32 %j.phi116156, bb %while.body38_preheader, i32 0]
  a.phi118163 = phi [bb %loop.while.exit, float %a.phi, bb %while.body38_preheader, float 0.000000]
  b.phi120162 = phi [bb %loop.while.exit, float %b.phi, bb %while.body38_preheader, float 1.000000]
  total.phi161 = phi [bb %loop.while.exit, float %88, bb %while.body38_preheader, float 0.000000]
  45 = srem i32 %i28.phi165, i32 10
  46 = icmp ne i32 %45, i32 0
  br i1 %46, bb %then, bb %else
while.exit39:  pred(loop.while.exit, entry27)
  160 = phi [bb %loop.while.exit, float %88, bb %entry27, float 0.000000]
  call void _sysy_stoptime(i32 39)
  95 = fsub float %160, float 19694630517866496.000000
  96 = f2si float %95 to i32
  98 = si2f i32 %96 to float
  99 = fcmp ole float %98, float 0.000001
  br i1 %99, bb %select.then, bb %select.else
then:  pred(while.body38)
  br bb %if.exit
if.exit:  pred(then, else)
  b.phi = phi [bb %else, float %57, bb %then, float 1.000000]
  a.phi = phi [bb %else, float %54, bb %then, float 0.000000]
  63155 = icmp slt i32 %j.phi116164, i32 4900
  br i1 %63155, bb %while.body59_preheader, bb %while.exit60
else:  pred(while.body38)
  54 = fadd float %a.phi118163, float 0.100000
  57 = fadd float %b.phi120162, float 0.200000
  br bb %if.exit
while.body59_preheader:  pred(if.exit)
  br bb %while.body59
while.body59:  pred(while.body59, while.body59_preheader)
  j.phi157 = phi [bb %while.body59, i32 %83, bb %while.body59_preheader, i32 %j.phi116164]
  125 = shl i32 %j.phi157, i32 2
  67 = ptradd ptr %x, i32 %125
  71 = si2f i32 %j.phi157 to float
  72 = fadd float %a.phi, float %71
  store float %72 to ptr %67
  75 = ptradd ptr %y, i32 %125
  80 = fadd float %b.phi, float %71
  store float %80 to ptr %75
  83 = add i32 %j.phi157, i32 1
  63 = icmp slt i32 %83, i32 4900
  br i1 %63, bb %while.body59, bb %while.exit60
while.exit60:  pred(while.body59, if.exit)
  j.phi116156 = phi [bb %while.body59, i32 %83, bb %if.exit, i32 %j.phi116164]
  br bb %loop.while.body
select.then:  pred(while.exit39)
  105 = fneg float 0.000001
  107 = fcmp oge float %98, float %105
  br bb %select.exit
select.else:  pred(while.exit39)
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 0, bb %select.then, i1 %107]
  br i1 %select.phi, bb %then109, bb %else111
then109:  pred(select.exit)
  call void putint(i32 10)
  ret i32 0
else111:  pred(select.exit)
  call void putint(i32 1)
  ret i32 1
loop.while.body:  pred(loop.while.body, while.exit60)
  i.phi134154 = phi [bb %loop.while.body, i32 %25145, bb %while.exit60, i32 0]
  accum.phi133153 = phi [bb %loop.while.body, float %22144, bb %while.exit60, float 0.000000]
  127138 = shl i32 %i.phi134154, i32 2
  15139 = ptradd ptr %x, i32 %127138
  15.load140 = load float from ptr %15139
  19141 = ptradd ptr %y, i32 %127138
  19.load142 = load float from ptr %19141
  21143 = fmul float %15.load140, float %19.load142
  22144 = fadd float %accum.phi133153, float %21143
  25145 = add i32 %i.phi134154, i32 1
  9135 = icmp slt i32 %25145, i32 4900
  br i1 %9135, bb %loop.while.body, bb %loop.while.exit
loop.while.exit:  pred(loop.while.body)
  152 = phi [bb %loop.while.body, float %22144]
  88 = fadd float %total.phi161, float %152
  91 = add i32 %i28.phi165, i32 1
  COUNT.load = load i32 from ptr @COUNT
  42 = icmp slt i32 %91, i32 %COUNT.load
  br i1 %42, bb %while.body38, bb %while.exit39
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getarray(ptr )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


