global ptr COUNT init with i32 500000
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getfarray(ptr )

i32 main() {
entry27:  pred()
  %y = stack 20000
  %x = stack 20000
  .pinned190 = pin float 0.000000
  call void _sysy_starttime(i32 22)
  COUNT.load167 = load i32 from ptr @COUNT
  42168 = icmp slt i32 0, i32 %COUNT.load167
  br i1 %42168, bb %while.body38_preheader, bb %while.exit39
while.body38_preheader:  pred(entry27)
  .pinned191 = pin float 1.000000
  .pinned = pin i32 5000
  br bb %while.body38
while.body38:  pred(loop.while.body_exclu_exit, while.body38_preheader)
  j.phi116174 = phi [bb %loop.while.body_exclu_exit, i32 %j.phi116165, bb %while.body38_preheader, i32 0]
  i28.phi173 = phi [bb %loop.while.body_exclu_exit, i32 %91, bb %while.body38_preheader, i32 0]
  total.phi172 = phi [bb %loop.while.body_exclu_exit, float %88, bb %while.body38_preheader, float %.pinned190]
  a.phi120171 = phi [bb %loop.while.body_exclu_exit, float %198, bb %while.body38_preheader, float %.pinned190]
  b.phi122170 = phi [bb %loop.while.body_exclu_exit, float %195, bb %while.body38_preheader, float %.pinned191]
  45 = srem i32 %i28.phi173, i32 10
  46 = icmp ne i32 %45, i32 0
  54 = fadd float %a.phi120171, float 0.100000
  57 = fadd float %b.phi122170, float 0.200000
  br i1 %46, bb %b.phi.which.true, bb %while.body38.split
while.body38.split:  pred(while.body38, b.phi.which.true)
  195 = phi [bb %b.phi.which.true, float %.pinned191, bb %while.body38, float %57]
  br i1 %46, bb %a.phi.which.true, bb %while.body38.split.split
while.body38.split.split:  pred(while.body38.split, a.phi.which.true)
  198 = phi [bb %a.phi.which.true, float %.pinned190, bb %while.body38.split, float %54]
  63164 = icmp slt i32 %j.phi116174, i32 %.pinned
  br i1 %63164, bb %while.body59_preheader, bb %while.exit60
while.exit39:  pred(entry27, while.body38_exclu_exit)
  169 = phi [bb %while.body38_exclu_exit, float %88, bb %entry27, float %.pinned190]
  .pinned192 = pin float 0.000001
  call void _sysy_stoptime(i32 39)
  95 = fsub float %169, float 20836462605893632.000000
  96 = f2si float %95 to i32
  98 = si2f i32 %96 to float
  99 = fcmp ole float %98, float %.pinned192
  105 = fneg float %.pinned192
  107 = fcmp oge float %98, float %105
  134 = and i1 %99, i1 %107
  br i1 %134, bb %then109, bb %else111
while.body59_preheader:  pred(while.body38.split.split)
  br bb %while.body59
while.body59:  pred(while.body59, while.body59_preheader)
  j.phi166 = phi [bb %while.body59, i32 %83, bb %while.body59_preheader, i32 %j.phi116174]
  158 = shl i32 %j.phi166, i32 2
  67 = ptradd ptr %x, i32 %158
  71 = si2f i32 %j.phi166 to float
  72 = fadd float %198, float %71
  store float %72 to ptr %67
  75 = ptradd ptr %y, i32 %158
  80 = fadd float %195, float %71
  store float %80 to ptr %75
  83 = add i32 %j.phi166, i32 1
  63 = icmp slt i32 %83, i32 %.pinned
  br i1 %63, bb %while.body59, bb %while.body59_exclu_exit
while.exit60:  pred(while.body38.split.split, while.body59_exclu_exit)
  j.phi116165 = phi [bb %while.body59_exclu_exit, i32 %83, bb %while.body38.split.split, i32 %j.phi116174]
  br bb %loop.while.body
then109:  pred(while.exit39)
  call void putint(i32 10)
  ret i32 0
else111:  pred(while.exit39)
  call void putint(i32 1)
  ret i32 1
loop.while.body:  pred(loop.while.body, while.exit60)
  i.phi140163 = phi [bb %loop.while.body, i32 %25151, bb %while.exit60, i32 0]
  accum.phi139162 = phi [bb %loop.while.body, float %22150, bb %while.exit60, float %.pinned190]
  159 = shl i32 %i.phi140163, i32 2
  15145 = ptradd ptr %x, i32 %159
  15.load146 = load float from ptr %15145
  19147 = ptradd ptr %y, i32 %159
  19.load148 = load float from ptr %19147
  21149 = fmul float %15.load146, float %19.load148
  22150 = fadd float %accum.phi139162, float %21149
  25151 = add i32 %i.phi140163, i32 1
  9141 = icmp slt i32 %25151, i32 %.pinned
  br i1 %9141, bb %loop.while.body, bb %loop.while.body_exclu_exit
while.body59_exclu_exit:  pred(while.body59)
  br bb %while.exit60
loop.while.body_exclu_exit:  pred(loop.while.body)
  88 = fadd float %total.phi172, float %22150
  91 = add i32 %i28.phi173, i32 1
  42 = icmp slt i32 %91, i32 %COUNT.load167
  br i1 %42, bb %while.body38, bb %while.body38_exclu_exit
while.body38_exclu_exit:  pred(loop.while.body_exclu_exit)
  br bb %while.exit39
b.phi.which.true:  pred(while.body38)
  br bb %while.body38.split
a.phi.which.true:  pred(while.body38.split)
  br bb %while.body38.split.split
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getarray(ptr )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


