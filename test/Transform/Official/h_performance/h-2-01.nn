global ptr COUNT init with i32 500000
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getfarray(ptr )

i32 main() {
entry27:  pred()
  %y = stack 20000
  %x = stack 20000
  .pinned = pin float 0.000000
  call void _sysy_starttime(i32 22)
  COUNT.load161 = load i32 from ptr @COUNT
  42162 = icmp slt i32 0, i32 %COUNT.load161
  br i1 %42162, bb %while.body38_preheader, bb %while.exit39
while.body38_preheader:  pred(entry27)
  .pinned184 = pin float 1.000000
  br bb %while.body38
while.body38:  pred(loop.while.body_exclu_exit, while.body38_preheader)
  j.phi115168 = phi [bb %loop.while.body_exclu_exit, i32 %j.phi115159, bb %while.body38_preheader, i32 0]
  i28.phi167 = phi [bb %loop.while.body_exclu_exit, i32 %91, bb %while.body38_preheader, i32 0]
  total.phi166 = phi [bb %loop.while.body_exclu_exit, float %88, bb %while.body38_preheader, float %.pinned]
  a.phi119165 = phi [bb %loop.while.body_exclu_exit, float %a.phi, bb %while.body38_preheader, float %.pinned]
  b.phi121164 = phi [bb %loop.while.body_exclu_exit, float %b.phi, bb %while.body38_preheader, float %.pinned184]
  45 = srem i32 %i28.phi167, i32 10
  46 = icmp ne i32 %45, i32 0
  br i1 %46, bb %then, bb %else
while.exit39:  pred(entry27, while.body38_exclu_exit)
  163 = phi [bb %while.body38_exclu_exit, float %88, bb %entry27, float %.pinned]
  .pinned185 = pin float 0.000001
  call void _sysy_stoptime(i32 39)
  95 = fsub float %163, float 20836462605893632.000000
  96 = f2si float %95 to i32
  98 = si2f i32 %96 to float
  99 = fcmp ole float %98, float %.pinned185
  br i1 %99, bb %select.then, bb %select.else
then:  pred(while.body38)
  br bb %if.exit
if.exit:  pred(then, else)
  b.phi = phi [bb %else, float %57, bb %then, float %.pinned184]
  a.phi = phi [bb %else, float %54, bb %then, float %.pinned]
  63158 = icmp slt i32 %j.phi115168, i32 5000
  br i1 %63158, bb %while.body59_preheader, bb %while.exit60
else:  pred(while.body38)
  54 = fadd float %a.phi119165, float 0.100000
  57 = fadd float %b.phi121164, float 0.200000
  br bb %if.exit
while.body59_preheader:  pred(if.exit)
  br bb %while.body59
while.body59:  pred(while.body59, while.body59_preheader)
  j.phi160 = phi [bb %while.body59, i32 %83, bb %while.body59_preheader, i32 %j.phi115168]
  152 = shl i32 %j.phi160, i32 2
  67 = ptradd ptr %x, i32 %152
  71 = si2f i32 %j.phi160 to float
  72 = fadd float %a.phi, float %71
  store float %72 to ptr %67
  75 = ptradd ptr %y, i32 %152
  80 = fadd float %b.phi, float %71
  store float %80 to ptr %75
  83 = add i32 %j.phi160, i32 1
  63 = icmp slt i32 %83, i32 5000
  br i1 %63, bb %while.body59, bb %while.body59_exclu_exit
while.exit60:  pred(if.exit, while.body59_exclu_exit)
  j.phi115159 = phi [bb %while.body59_exclu_exit, i32 %83, bb %if.exit, i32 %j.phi115168]
  br bb %loop.while.body
select.then:  pred(while.exit39)
  105 = fneg float %.pinned185
  107 = fcmp oge float %98, float %105
  br bb %select.exit
select.else:  pred(while.exit39)
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 0, bb %select.then, i1 %107]
  br i1 %select.phi, bb %then109, bb %else111
then109:  pred(select.exit)
  call void putint(i32 10)
  ret i32 0
else111:  pred(select.exit)
  call void putint(i32 1)
  ret i32 1
loop.while.body:  pred(loop.while.body, while.exit60)
  i.phi134157 = phi [bb %loop.while.body, i32 %25145, bb %while.exit60, i32 0]
  accum.phi133156 = phi [bb %loop.while.body, float %22144, bb %while.exit60, float %.pinned]
  153 = shl i32 %i.phi134157, i32 2
  15139 = ptradd ptr %x, i32 %153
  15.load140 = load float from ptr %15139
  19141 = ptradd ptr %y, i32 %153
  19.load142 = load float from ptr %19141
  21143 = fmul float %15.load140, float %19.load142
  22144 = fadd float %accum.phi133156, float %21143
  25145 = add i32 %i.phi134157, i32 1
  9135 = icmp slt i32 %25145, i32 5000
  br i1 %9135, bb %loop.while.body, bb %loop.while.body_exclu_exit
while.body59_exclu_exit:  pred(while.body59)
  br bb %while.exit60
loop.while.body_exclu_exit:  pred(loop.while.body)
  88 = fadd float %total.phi166, float %22144
  91 = add i32 %i28.phi167, i32 1
  42 = icmp slt i32 %91, i32 %COUNT.load161
  br i1 %42, bb %while.body38, bb %while.body38_exclu_exit
while.body38_exclu_exit:  pred(loop.while.body_exclu_exit)
  br bb %while.exit39
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getarray(ptr )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


