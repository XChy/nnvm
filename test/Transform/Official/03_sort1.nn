global ptr ans init with i32 0
global ptr a init with 30000010 x i32 zeros
immutable global ptr base init with i32 16
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry311:
  313 = call i32 getarray(ptr @a)
  call void _sysy_starttime(i32 90)
  call void radixSort(i32 8, ptr @a, i32 0, i32 %313)
  br bb %while.cond318
while.cond318:
  i317.phi = phi [bb %entry311, i32 0, bb %while.body319, i32 %337]
  323 = icmp slt i32 %i317.phi, i32 %313
  br i1 %323, bb %while.body319, bb %while.exit320
while.body319:
  ans.load = load i32 from ptr @ans
  368 = shl i32 %i317.phi, i32 2
  329 = ptradd ptr @a, i32 %368
  329.load = load i32 from ptr %329
  369 = add i32 %i317.phi, i32 2
  333 = srem i32 %329.load, i32 %369
  334 = mul i32 %i317.phi, i32 %333
  335 = add i32 %ans.load, i32 %334
  store i32 %335 to ptr @ans
  337 = add i32 %i317.phi, i32 1
  br bb %while.cond318
while.exit320:
  ans.load338 = load i32 from ptr @ans
  339 = icmp slt i32 %ans.load338, i32 0
  br i1 %339, bb %then340, bb %if.exit341
then340:
  ans.load342 = load i32 from ptr @ans
  343 = sub i32 0, i32 %ans.load342
  store i32 %343 to ptr @ans
  br bb %if.exit341
if.exit341:
  call void _sysy_stoptime(i32 102)
  ans.load345 = load i32 from ptr @ans
  call void putint(i32 %ans.load345)
  call void putch(i32 10)
  ret i32 0
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

pure i32 getNumPos(i32 num, i32 pos) {
entry26:
  br bb %while.cond31
while.cond31:
  i30.phi = phi [bb %entry26, i32 0, bb %while.body32, i32 %42]
  num.stack.phi = phi [bb %entry26, i32 %num, bb %while.body32, i32 %40]
  36 = icmp slt i32 %i30.phi, i32 %pos
  br i1 %36, bb %while.body32, bb %while.exit33
while.body32:
  40 = sdiv i32 %num.stack.phi, i32 16
  42 = add i32 %i30.phi, i32 1
  br bb %while.cond31
while.exit33:
  45 = srem i32 %num.stack.phi, i32 16
  ret i32 %45
}

external void _sysy_starttime(i32 )

void radixSort(i32 bitround, ptr a, i32 l, i32 r) {
entry46:
  %cnt74 = stack 64
  %tail = stack 64
  %head = stack 64
  br bb %init.cond
init.cond:
  cnt_ptr.phi = phi [bb %entry46, i32 16, bb %init, i32 %cnt.dec]
  56 = icmp ne i32 %cnt_ptr.phi, i32 0
  br i1 %56, bb %init, bb %init.exit
init:
  cnt.dec = sub i32 %cnt_ptr.phi, i32 1
  370 = shl i32 %cnt.dec, i32 2
  arr.index61 = ptradd ptr %head, i32 %370
  store i32 0 to ptr %arr.index61
  br bb %init.cond
init.exit:
  br bb %init.cond63
init.cond63:
  cnt_ptr66.phi = phi [bb %init.exit, i32 16, bb %init64, i32 %cnt.dec70]
  68 = icmp ne i32 %cnt_ptr66.phi, i32 0
  br i1 %68, bb %init64, bb %init.exit65
init64:
  cnt.dec70 = sub i32 %cnt_ptr66.phi, i32 1
  372 = shl i32 %cnt.dec70, i32 2
  arr.index73 = ptradd ptr %tail, i32 %372
  store i32 0 to ptr %arr.index73
  br bb %init.cond63
init.exit65:
  br bb %init.cond75
init.cond75:
  cnt_ptr78.phi = phi [bb %init.exit65, i32 16, bb %init76, i32 %cnt.dec82]
  80 = icmp ne i32 %cnt_ptr78.phi, i32 0
  br i1 %80, bb %init76, bb %init.exit77
init76:
  cnt.dec82 = sub i32 %cnt_ptr78.phi, i32 1
  374 = shl i32 %cnt.dec82, i32 2
  arr.index85 = ptradd ptr %cnt74, i32 %374
  store i32 0 to ptr %arr.index85
  br bb %init.cond75
init.exit77:
  88 = icmp eq i32 %bitround, i32 -1
  br i1 %88, bb %select.then, bb %select.else
select.then:
  br bb %select.exit
select.else:
  94 = add i32 %l, i32 1
  96 = icmp sge i32 %94, i32 %r
  br bb %select.exit
select.exit:
  select.phi = phi [bb %select.else, i1 %96, bb %select.then, i1 -1]
  br i1 %select.phi, bb %then99, bb %if.exit100
then99:
  ret 
if.exit100:
  br bb %while.cond103
while.cond103:
  i101.phi357 = phi [bb %if.exit100, i32 %l, bb %getNumPos.while.exit33, i32 %129]
  108 = icmp slt i32 %i101.phi357, i32 %r
  br i1 %108, bb %while.body104, bb %while.exit105
while.body104:
  376 = shl i32 %i101.phi357, i32 2
  112 = ptradd ptr %a, i32 %376
  112.load = load i32 from ptr %112
  br bb %getNumPos.while.cond31
while.exit105:
  store i32 %l to ptr %head
  137.load = load i32 from ptr %cnt74
  139 = add i32 %l, i32 %137.load
  store i32 %139 to ptr %tail
  br bb %while.cond140
while.cond140:
  i101.phi356 = phi [bb %while.exit105, i32 1, bb %while.body141, i32 %168]
  145 = icmp slt i32 %i101.phi356, i32 16
  br i1 %145, bb %while.body141, bb %while.exit142
while.body141:
  380 = shl i32 %i101.phi356, i32 2
  149 = ptradd ptr %head, i32 %380
  151 = sub i32 %i101.phi356, i32 1
  381 = shl i32 %151, i32 2
  153 = ptradd ptr %tail, i32 %381
  153.load = load i32 from ptr %153
  store i32 %153.load to ptr %149
  157 = ptradd ptr %tail, i32 %380
  164 = ptradd ptr %cnt74, i32 %380
  164.load = load i32 from ptr %164
  166 = add i32 %153.load, i32 %164.load
  store i32 %166 to ptr %157
  168 = add i32 %i101.phi356, i32 1
  br bb %while.cond140
while.exit142:
  br bb %while.cond169
while.cond169:
  t.phi363 = phi [bb %while.exit142, i32 ub, bb %while.exit178, i32 %t.phi362]
  v.phi360 = phi [bb %while.exit142, i32 ub, bb %while.exit178, i32 %v.phi359]
  i101.phi = phi [bb %while.exit142, i32 0, bb %while.exit178, i32 %254]
  174 = icmp slt i32 %i101.phi, i32 16
  br i1 %174, bb %while.body170, bb %while.exit171
while.body170:
  385 = shl i32 %i101.phi, i32 2
  br bb %while.cond176
while.exit171:
  store i32 %l to ptr %head
  store i32 %139 to ptr %tail
  br bb %while.cond267
while.cond176:
  t.phi362 = phi [bb %while.body170, i32 %t.phi363, bb %while.exit199, i32 %t.phi]
  v.phi359 = phi [bb %while.body170, i32 %v.phi360, bb %while.exit199, i32 %v.phi]
  181 = ptradd ptr %head, i32 %385
  181.load = load i32 from ptr %181
  185 = ptradd ptr %tail, i32 %385
  185.load = load i32 from ptr %185
  187 = icmp slt i32 %181.load, i32 %185.load
  br i1 %187, bb %while.body177, bb %while.exit178
while.body177:
  388 = shl i32 %181.load, i32 2
  195 = ptradd ptr %a, i32 %388
  195.load = load i32 from ptr %195
  br bb %while.cond197
while.exit178:
  254 = add i32 %i101.phi, i32 1
  br bb %while.cond169
while.cond197:
  t.phi = phi [bb %while.body177, i32 %t.phi362, bb %while.body198, i32 %v.phi]
  v.phi = phi [bb %while.body177, i32 %195.load, bb %while.body198, i32 %215.load]
  br bb %getNumPos.while.cond31427
while.body198:
  389 = shl i32 %45437, i32 2
  212 = ptradd ptr %head, i32 %389
  212.load = load i32 from ptr %212
  390 = shl i32 %212.load, i32 2
  215 = ptradd ptr %a, i32 %390
  215.load = load i32 from ptr %215
  store i32 %v.phi to ptr %215
  237 = add i32 %212.load, i32 1
  store i32 %237 to ptr %212
  br bb %while.cond197
while.exit199:
  240.load = load i32 from ptr %181
  396 = shl i32 %240.load, i32 2
  243 = ptradd ptr %a, i32 %396
  store i32 %v.phi to ptr %243
  252 = add i32 %240.load, i32 1
  store i32 %252 to ptr %181
  br bb %while.cond176
while.cond267:
  i255.phi = phi [bb %while.exit171, i32 0, bb %if.exit277, i32 %310]
  272 = icmp slt i32 %i255.phi, i32 16
  br i1 %272, bb %while.body268, bb %while.exit269
while.body268:
  275 = icmp sgt i32 %i255.phi, i32 0
  br i1 %275, bb %then276, bb %if.exit277
while.exit269:
  ret 
then276:
  399 = shl i32 %i255.phi, i32 2
  280 = ptradd ptr %head, i32 %399
  282 = sub i32 %i255.phi, i32 1
  400 = shl i32 %282, i32 2
  284 = ptradd ptr %tail, i32 %400
  284.load = load i32 from ptr %284
  store i32 %284.load to ptr %280
  288 = ptradd ptr %tail, i32 %399
  295 = ptradd ptr %cnt74, i32 %399
  295.load = load i32 from ptr %295
  297 = add i32 %284.load, i32 %295.load
  store i32 %297 to ptr %288
  br bb %if.exit277
if.exit277:
  299 = sub i32 %bitround, i32 1
  404 = shl i32 %i255.phi, i32 2
  302 = ptradd ptr %head, i32 %404
  302.load = load i32 from ptr %302
  306 = ptradd ptr %tail, i32 %404
  306.load = load i32 from ptr %306
  call void radixSort(i32 %299, ptr %a, i32 %302.load, i32 %306.load)
  310 = add i32 %i255.phi, i32 1
  br bb %while.cond267
getNumPos.while.cond31:
  i30.phi412 = phi [bb %while.body104, i32 0, bb %getNumPos.while.body32, i32 %42418]
  num.stack.phi413 = phi [bb %while.body104, i32 %112.load, bb %getNumPos.while.body32, i32 %40417]
  36414 = icmp slt i32 %i30.phi412, i32 %bitround
  br i1 %36414, bb %getNumPos.while.body32, bb %getNumPos.while.exit33
getNumPos.while.body32:
  40417 = sdiv i32 %num.stack.phi413, i32 16
  42418 = add i32 %i30.phi412, i32 1
  br bb %getNumPos.while.cond31
getNumPos.while.exit33:
  45421 = srem i32 %num.stack.phi413, i32 16
  377 = shl i32 %45421, i32 2
  117 = ptradd ptr %cnt74, i32 %377
  125.load = load i32 from ptr %117
  127 = add i32 %125.load, i32 1
  store i32 %127 to ptr %117
  129 = add i32 %i101.phi357, i32 1
  br bb %while.cond103
getNumPos.while.cond31427:
  i30.phi428 = phi [bb %while.cond197, i32 0, bb %getNumPos.while.body32432, i32 %42434]
  num.stack.phi429 = phi [bb %while.cond197, i32 %v.phi, bb %getNumPos.while.body32432, i32 %40433]
  36430 = icmp slt i32 %i30.phi428, i32 %bitround
  br i1 %36430, bb %getNumPos.while.body32432, bb %getNumPos.while.exit33436
getNumPos.while.body32432:
  40433 = sdiv i32 %num.stack.phi429, i32 16
  42434 = add i32 %i30.phi428, i32 1
  br bb %getNumPos.while.cond31427
getNumPos.while.exit33436:
  45437 = srem i32 %num.stack.phi429, i32 16
  204 = icmp ne i32 %45437, i32 %i101.phi
  br i1 %204, bb %while.body198, bb %while.exit199
}

i32 getMaxNum(i32 n, ptr arr) {
entry:
  br bb %while.cond
while.cond:
  i.phi = phi [bb %entry, i32 0, bb %if.exit, i32 %24]
  ret.phi366 = phi [bb %entry, i32 0, bb %if.exit, i32 %ret.phi]
  9 = icmp slt i32 %i.phi, i32 %n
  br i1 %9, bb %while.body, bb %while.exit
while.body:
  406 = shl i32 %i.phi, i32 2
  13 = ptradd ptr %arr, i32 %406
  13.load = load i32 from ptr %13
  16 = icmp sgt i32 %13.load, i32 %ret.phi366
  br i1 %16, bb %then, bb %if.exit
while.exit:
  ret i32 %ret.phi366
then:
  br bb %if.exit
if.exit:
  ret.phi = phi [bb %while.body, i32 %ret.phi366, bb %then, i32 %13.load]
  24 = add i32 %i.phi, i32 1
  br bb %while.cond
}

external void putfarray(i32 , ptr )


