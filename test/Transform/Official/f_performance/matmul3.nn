internal global ptr c init with 1000000 x i32 zeros
internal global ptr b init with 1000000 x i32 zeros
internal global ptr a init with 1000000 x i32 zeros
i32 main() {
entry:  pred()
  .pinned = pin i32 4000
  a.pinned = pin ptr @a
  br bb %while.cond
while.cond:  pred(entry, if.exit)
  496 = phi [bb %entry, i32 0, bb %if.exit, i32 %15.rec]
  i.phi256 = phi [bb %entry, i32 0, bb %if.exit, i32 %25]
  11 = icmp slt i32 %i.phi256, i32 1000
  br i1 %11, bb %while.body, bb %while.exit
while.body:  pred(while.cond)
  16 = ptradd ptr %a.pinned, i32 %496
  17 = call i32 getarray(ptr %16)
  19 = icmp ne i32 %17, i32 1000
  br i1 %19, bb %then, bb %if.exit
while.exit:  pred(while.cond)
  b.pinned = pin ptr @b
  call void _sysy_starttime(i32 23)
  br bb %while.cond35_preheader
then:  pred(while.body)
  ret i32 %17
if.exit:  pred(while.body)
  25 = add i32 %i.phi256, i32 1
  15.rec = add i32 %496, i32 %.pinned
  br bb %while.cond
while.cond35_preheader:  pred(while.exit37, while.exit)
  475 = phi [bb %while.exit, i32 0, bb %while.exit37, i32 %42.rec]
  i.phi255397 = phi [bb %while.exit37, i32 %60, bb %while.exit, i32 0]
  43 = ptradd ptr %b.pinned, i32 %475
  301 = shl i32 %i.phi255397, i32 2
  468 = ptradd ptr %a.pinned, i32 %301
  br bb %while.body36
while.body36:  pred(while.body36, while.cond35_preheader)
  472 = phi [bb %while.cond35_preheader, i32 0, bb %while.body36, i32 %49.rec]
  j.phi266351 = phi [bb %while.body36, i32 %57, bb %while.cond35_preheader, i32 0]
  300 = shl i32 %j.phi266351, i32 2
  46 = ptradd ptr %43, i32 %300
  469 = ptradd ptr %468, i32 %472
  53.load = load i32 from ptr %469
  store i32 %53.load to ptr %46
  57 = add i32 %j.phi266351, i32 1
  39 = icmp slt i32 %57, i32 1000
  49.rec = add i32 %472, i32 %.pinned
  br i1 %39, bb %while.body36, bb %while.exit37
while.exit37:  pred(while.body36)
  60 = add i32 %i.phi255397, i32 1
  32 = icmp slt i32 %60, i32 1000
  42.rec = add i32 %475, i32 %.pinned
  br i1 %32, bb %while.cond35_preheader, bb %while.cond69_preheader
while.cond69_preheader:  pred(while.exit71, while.exit37)
  481 = phi [bb %while.exit71, i32 %86353.rec, bb %while.exit37, i32 0]
  i.phi254395 = phi [bb %while.exit71, i32 %117, bb %while.exit37, i32 0]
  c.pinned = pin ptr @c
  br bb %while.cond77_preheader
while.exit71:  pred(while.exit79)
  117 = add i32 %i.phi254395, i32 1
  66 = icmp slt i32 %117, i32 1000
  86353.rec = add i32 %481, i32 %.pinned
  br i1 %66, bb %while.cond69_preheader, bb %while.cond128_preheader
while.cond77_preheader:  pred(while.exit79, while.cond69_preheader)
  j.phi264368 = phi [bb %while.exit79, i32 %114, bb %while.cond69_preheader, i32 0]
  303354 = shl i32 %j.phi264368, i32 2
  br bb %while.body78
while.body78:  pred(while.body78, while.cond77_preheader)
  478 = phi [bb %while.cond77_preheader, i32 0, bb %while.body78, i32 %93.rec]
  k.phi360 = phi [bb %while.body78, i32 %103, bb %while.cond77_preheader, i32 0]
  temp.phi359 = phi [bb %while.body78, i32 %100, bb %while.cond77_preheader, i32 0]
  87 = ptradd ptr %a.pinned, i32 %481
  302 = shl i32 %k.phi360, i32 2
  90 = ptradd ptr %87, i32 %302
  90.load = load i32 from ptr %90
  94 = ptradd ptr %b.pinned, i32 %478
  97 = ptradd ptr %94, i32 %303354
  97.load = load i32 from ptr %97
  99 = mul i32 %90.load, i32 %97.load
  100 = add i32 %temp.phi359, i32 %99
  103 = add i32 %k.phi360, i32 1
  81 = icmp slt i32 %103, i32 1000
  93.rec = add i32 %478, i32 %.pinned
  br i1 %81, bb %while.body78, bb %while.exit79
while.exit79:  pred(while.body78)
  106 = ptradd ptr %c.pinned, i32 %481
  109 = ptradd ptr %106, i32 %303354
  store i32 %100 to ptr %109
  114 = add i32 %j.phi264368, i32 1
  73 = icmp slt i32 %114, i32 1000
  br i1 %73, bb %while.cond77_preheader, bb %while.exit71
while.cond128_preheader:  pred(while.exit159, while.exit71)
  484 = phi [bb %while.exit159, i32 %135372.rec, bb %while.exit71, i32 0]
  i.phi253391 = phi [bb %while.exit159, i32 %176, bb %while.exit71, i32 0]
  br bb %while.body129
while.body129:  pred(while.body129, while.cond128_preheader)
  j.phi263376 = phi [bb %while.body129, i32 %155, bb %while.cond128_preheader, i32 0]
  temp126.phi277375 = phi [bb %while.body129, i32 %523, bb %while.cond128_preheader, i32 2147483647]
  136 = ptradd ptr %c.pinned, i32 %484
  305 = shl i32 %j.phi263376, i32 2
  139 = ptradd ptr %136, i32 %305
  139.load = load i32 from ptr %139
  523 = signed min i32 %139.load, i32 %temp126.phi277375
  155 = add i32 %j.phi263376, i32 1
  132 = icmp slt i32 %155, i32 1000
  br i1 %132, bb %while.body129, bb %while.cond157_preheader
while.cond157_preheader:  pred(while.body129)
  br bb %while.body158
while.body158:  pred(while.body158, while.cond157_preheader)
  j.phi261370 = phi [bb %while.body158, i32 %173, bb %while.cond157_preheader, i32 0]
  306 = shl i32 %j.phi261370, i32 2
  168 = ptradd ptr %136, i32 %306
  store i32 %523 to ptr %168
  173 = add i32 %j.phi261370, i32 1
  161 = icmp slt i32 %173, i32 1000
  br i1 %161, bb %while.body158, bb %while.exit159
while.exit159:  pred(while.body158)
  176 = add i32 %i.phi253391, i32 1
  123 = icmp slt i32 %176, i32 1000
  135372.rec = add i32 %484, i32 %.pinned
  br i1 %123, bb %while.cond128_preheader, bb %while.cond187_preheader
while.cond187_preheader:  pred(while.exit189, while.exit159)
  490 = phi [bb %while.exit189, i32 %194.rec, bb %while.exit159, i32 0]
  i.phi252389 = phi [bb %while.exit189, i32 %213, bb %while.exit159, i32 0]
  195 = ptradd ptr %c.pinned, i32 %490
  308 = shl i32 %i.phi252389, i32 2
  470 = ptradd ptr %c.pinned, i32 %308
  br bb %while.body188
while.body188:  pred(while.body188, while.cond187_preheader)
  487 = phi [bb %while.cond187_preheader, i32 0, bb %while.body188, i32 %201.rec]
  j.phi259379 = phi [bb %while.body188, i32 %210, bb %while.cond187_preheader, i32 0]
  307 = shl i32 %j.phi259379, i32 2
  198 = ptradd ptr %195, i32 %307
  471 = ptradd ptr %470, i32 %487
  205.load = load i32 from ptr %471
  207 = sub i32 0, i32 %205.load
  store i32 %207 to ptr %198
  210 = add i32 %j.phi259379, i32 1
  191 = icmp slt i32 %210, i32 1000
  201.rec = add i32 %487, i32 %.pinned
  br i1 %191, bb %while.body188, bb %while.exit189
while.exit189:  pred(while.body188)
  213 = add i32 %i.phi252389, i32 1
  182 = icmp slt i32 %213, i32 1000
  194.rec = add i32 %490, i32 %.pinned
  br i1 %182, bb %while.cond187_preheader, bb %while.cond224_preheader
while.exit217:  pred(while.exit226)
  call void _sysy_stoptime(i32 92)
  call void putint(i32 %239)
  ret i32 0
while.cond224_preheader:  pred(while.exit226, while.exit189)
  493 = phi [bb %while.exit226, i32 %233.rec, bb %while.exit189, i32 0]
  i.phi387 = phi [bb %while.exit226, i32 %245, bb %while.exit189, i32 0]
  sum.phi272386 = phi [bb %while.exit226, i32 %239, bb %while.exit189, i32 0]
  234 = ptradd ptr %c.pinned, i32 %493
  br bb %while.body225
while.body225:  pred(while.body225, while.cond224_preheader)
  j.phi383 = phi [bb %while.body225, i32 %242, bb %while.cond224_preheader, i32 0]
  sum.phi382 = phi [bb %while.body225, i32 %239, bb %while.cond224_preheader, i32 %sum.phi272386]
  309 = shl i32 %j.phi383, i32 2
  237 = ptradd ptr %234, i32 %309
  237.load = load i32 from ptr %237
  239 = add i32 %sum.phi382, i32 %237.load
  242 = add i32 %j.phi383, i32 1
  228 = icmp slt i32 %242, i32 1000
  br i1 %228, bb %while.body225, bb %while.exit226
while.exit226:  pred(while.body225)
  245 = add i32 %i.phi387, i32 1
  219 = icmp slt i32 %245, i32 1000
  233.rec = add i32 %493, i32 %.pinned
  br i1 %219, bb %while.cond224_preheader, bb %while.exit217
}

external void _sysy_starttime(i32 )

external void _sysy_stoptime(i32 )

external i32 getarray(ptr )

external void putint(i32 )


