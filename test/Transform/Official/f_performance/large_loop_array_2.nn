global ptr COUNT init with i32 500000
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getfarray(ptr )

i32 main() {
entry27:  pred()
  %y = stack 16384
  %x = stack 16384
  .pinned = pin float 0.000000
  31 = call i32 getint()
  call void _sysy_starttime(i32 22)
  COUNT.load159 = load i32 from ptr @COUNT
  43160 = icmp slt i32 0, i32 %COUNT.load159
  br i1 %43160, bb %while.body39_preheader, bb %while.exit40
while.body39_preheader:  pred(entry27)
  .pinned182 = pin float 1.000000
  9133152 = icmp slt i32 0, i32 %31
  br bb %while.body39
while.body39:  pred(loop.while.exit, while.body39_preheader)
  i28.phi166 = phi [bb %loop.while.exit, i32 %92, bb %while.body39_preheader, i32 0]
  j.phi114165 = phi [bb %loop.while.exit, i32 %j.phi114157, bb %while.body39_preheader, i32 0]
  total.phi164 = phi [bb %loop.while.exit, float %89, bb %while.body39_preheader, float %.pinned]
  a.phi117163 = phi [bb %loop.while.exit, float %a.phi, bb %while.body39_preheader, float %.pinned]
  b.phi119162 = phi [bb %loop.while.exit, float %b.phi, bb %while.body39_preheader, float %.pinned182]
  46 = srem i32 %i28.phi166, i32 10
  47 = icmp ne i32 %46, i32 0
  br i1 %47, bb %then, bb %else
while.exit40:  pred(entry27, while.body39_exclu_exit)
  161 = phi [bb %while.body39_exclu_exit, float %89, bb %entry27, float %.pinned]
  .pinned183 = pin float 0.000001
  call void _sysy_stoptime(i32 39)
  95 = fsub float %161, float 11442437121638400.000000
  96 = fcmp ole float %95, float %.pinned183
  br i1 %96, bb %select.then, bb %select.else
then:  pred(while.body39)
  br bb %if.exit
if.exit:  pred(then, else)
  b.phi = phi [bb %else, float %58, bb %then, float %.pinned182]
  a.phi = phi [bb %else, float %55, bb %then, float %.pinned]
  64156 = icmp slt i32 %j.phi114165, i32 %31
  br i1 %64156, bb %while.body60_preheader, bb %while.exit61
else:  pred(while.body39)
  55 = fadd float %a.phi117163, float 0.100000
  58 = fadd float %b.phi119162, float 0.200000
  br bb %if.exit
while.body60_preheader:  pred(if.exit)
  br bb %while.body60
while.body60:  pred(while.body60, while.body60_preheader)
  j.phi158 = phi [bb %while.body60, i32 %84, bb %while.body60_preheader, i32 %j.phi114165]
  150 = shl i32 %j.phi158, i32 2
  68 = ptradd ptr %x, i32 %150
  72 = si2f i32 %j.phi158 to float
  73 = fadd float %a.phi, float %72
  store float %73 to ptr %68
  76 = ptradd ptr %y, i32 %150
  81 = fadd float %b.phi, float %72
  store float %81 to ptr %76
  84 = add i32 %j.phi158, i32 1
  64 = icmp slt i32 %84, i32 %31
  br i1 %64, bb %while.body60, bb %while.body60_exclu_exit
while.exit61:  pred(if.exit, while.body60_exclu_exit)
  j.phi114157 = phi [bb %while.body60_exclu_exit, i32 %84, bb %if.exit, i32 %j.phi114165]
  br i1 %9133152, bb %loop.while.body_preheader, bb %loop.while.exit
select.then:  pred(while.exit40)
  br bb %select.exit
select.else:  pred(while.exit40)
  103 = fneg float %.pinned183
  104 = fcmp oge float %95, float %103
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 %104, bb %select.then, i1 -1]
  br i1 %select.phi, bb %then107, bb %else109
then107:  pred(select.exit)
  call void putint(i32 0)
  ret i32 0
else109:  pred(select.exit)
  call void putint(i32 1)
  ret i32 1
loop.while.body_preheader:  pred(while.exit61)
  br bb %loop.while.body
loop.while.body:  pred(loop.while.body, loop.while.body_preheader)
  i.phi132155 = phi [bb %loop.while.body, i32 %25143, bb %loop.while.body_preheader, i32 0]
  accumulator.phi131154 = phi [bb %loop.while.body, float %22142, bb %loop.while.body_preheader, float %.pinned]
  151 = shl i32 %i.phi132155, i32 2
  15137 = ptradd ptr %x, i32 %151
  15.load138 = load float from ptr %15137
  19139 = ptradd ptr %y, i32 %151
  19.load140 = load float from ptr %19139
  21141 = fmul float %15.load138, float %19.load140
  22142 = fadd float %accumulator.phi131154, float %21141
  25143 = add i32 %i.phi132155, i32 1
  9133 = icmp slt i32 %25143, i32 %31
  br i1 %9133, bb %loop.while.body, bb %loop.while.body_exclu_exit
loop.while.exit:  pred(while.exit61, loop.while.body_exclu_exit)
  153 = phi [bb %loop.while.body_exclu_exit, float %22142, bb %while.exit61, float %.pinned]
  89 = fadd float %total.phi164, float %153
  92 = add i32 %i28.phi166, i32 1
  43 = icmp slt i32 %92, i32 %COUNT.load159
  br i1 %43, bb %while.body39, bb %while.body39_exclu_exit
while.body60_exclu_exit:  pred(while.body60)
  br bb %while.exit61
loop.while.body_exclu_exit:  pred(loop.while.body)
  br bb %loop.while.exit
while.body39_exclu_exit:  pred(loop.while.exit)
  br bb %while.exit40
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getarray(ptr )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


