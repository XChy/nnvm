internal global ptr a init with 100000 x i32 zeros
internal global ptr matrix init with 20000000 x i32 zeros
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry73:  pred()
  a.pinned = pin ptr @a
  matrix.pinned = pin ptr @matrix
  75 = call i32 getint()
  77 = call i32 getarray(ptr %a.pinned)
  call void _sysy_starttime(i32 28)
  85225 = icmp slt i32 0, i32 %75
  br i1 %85225, bb %while.body81_preheader, bb %while.exit82
while.body81_preheader:  pred(entry73)
  br bb %while.body81
while.body81:  pred(while.body81, while.body81_preheader)
  i79.phi148226 = phi [bb %while.body81, i32 %94, bb %while.body81_preheader, i32 0]
  211 = shl i32 %i79.phi148226, i32 2
  89 = ptradd ptr %matrix.pinned, i32 %211
  store i32 %i79.phi148226 to ptr %89
  94 = add i32 %i79.phi148226, i32 1
  85 = icmp slt i32 %94, i32 %75
  br i1 %85, bb %while.body81, bb %while.exit82
while.exit82:  pred(entry73, while.body81)
  101223 = icmp slt i32 0, i32 %77
  br i1 %101223, bb %while.body97_preheader, bb %while.exit98
while.body97_preheader:  pred(while.exit82)
  br bb %while.body97
while.body97:  pred(transpose.while.exit, while.body97_preheader)
  i79.phi147224 = phi [bb %transpose.while.exit, i32 %111, bb %while.body97_preheader, i32 0]
  212 = shl i32 %i79.phi147224, i32 2
  106 = ptradd ptr %a.pinned, i32 %212
  106.load = load i32 from ptr %106
  6166 = sdiv i32 %75, i32 %106.load
  14171216 = icmp slt i32 0, i32 %6166
  br i1 %14171216, bb %transpose.while.body_preheader, bb %transpose.while.exit
while.exit98:  pred(while.exit82, transpose.while.exit)
  br i1 %101223, bb %while.body115_preheader, bb %while.exit116
while.body115_preheader:  pred(while.exit98)
  br bb %while.body115
while.body115:  pred(while.body115, while.body115_preheader)
  i79.phi222 = phi [bb %while.body115, i32 %134, bb %while.body115_preheader, i32 0]
  ans.phi150221 = phi [bb %while.body115, i32 %131, bb %while.body115_preheader, i32 0]
  125 = mul i32 %i79.phi222, i32 %i79.phi222
  213 = shl i32 %i79.phi222, i32 2
  128 = ptradd ptr %matrix.pinned, i32 %213
  128.load = load i32 from ptr %128
  130 = mul i32 %125, i32 %128.load
  131 = add i32 %ans.phi150221, i32 %130
  134 = add i32 %i79.phi222, i32 1
  119 = icmp slt i32 %134, i32 %77
  br i1 %119, bb %while.body115, bb %while.body115_exclu_exit
while.exit116:  pred(while.exit98, while.body115_exclu_exit)
  220 = phi [bb %while.body115_exclu_exit, i32 %131, bb %while.exit98, i32 0]
  136 = icmp slt i32 %220, i32 0
  141 = sub i32 0, i32 %220
  br i1 %136, bb %ans.phi.which.true, bb %while.exit116.split
while.exit116.split:  pred(while.exit116, ans.phi.which.true)
  255 = phi [bb %ans.phi.which.true, i32 %141, bb %while.exit116, i32 %220]
  call void _sysy_stoptime(i32 47)
  call void putint(i32 %255)
  call void putch(i32 10)
  ret i32 0
transpose.while.body_preheader:  pred(while.body97)
  br bb %transpose.while.body
transpose.while.body:  pred(transpose.while.exit19, transpose.while.body_preheader)
  246 = phi [bb %transpose.while.body_preheader, i32 0, bb %transpose.while.exit19, i32 %35192.rec]
  i.phi170218 = phi [bb %transpose.while.exit19, i32 %71186, bb %transpose.while.body_preheader, i32 0]
  curr.phi155169217 = phi [bb %transpose.while.exit19, i32 %curr.phi178, bb %transpose.while.body_preheader, i32 0]
  br bb %transpose.while.cond17
transpose.while.exit:  pred(while.body97, transpose.while.exit19)
  111 = add i32 %i79.phi147224, i32 1
  101 = icmp slt i32 %111, i32 %77
  br i1 %101, bb %while.body97, bb %while.exit98
transpose.while.cond17:  pred(transpose.while.body, transpose.while.cond17_unified_latch)
  244 = phi [bb %transpose.while.body, i32 0, bb %transpose.while.cond17_unified_latch, i32 %43197.rec]
  curr.phi178 = phi [bb %transpose.while.body, i32 %curr.phi155169217, bb %transpose.while.cond17_unified_latch, i32 %231]
  j.phi179 = phi [bb %transpose.while.body, i32 0, bb %transpose.while.cond17_unified_latch, i32 %31189]
  22180 = icmp slt i32 %j.phi179, i32 %106.load
  br i1 %22180, bb %transpose.while.body18, bb %transpose.while.exit19
transpose.while.body18:  pred(transpose.while.cond17)
  26183 = icmp slt i32 %i.phi170218, i32 %j.phi179
  31189 = add i32 %j.phi179, i32 1
  br i1 %26183, bb %transpose.then, bb %transpose.if.exit
transpose.while.exit19:  pred(transpose.while.cond17)
  71186 = add i32 %i.phi170218, i32 1
  14171 = icmp slt i32 %71186, i32 %6166
  35192.rec = add i32 %246, i32 %106.load
  br i1 %14171, bb %transpose.while.body, bb %transpose.while.exit
transpose.then:  pred(transpose.while.body18)
  br bb %transpose.while.cond17_unified_latch
transpose.if.exit:  pred(transpose.while.body18)
  37193 = add i32 %246, i32 %j.phi179
  214 = shl i32 %37193, i32 2
  39195 = ptradd ptr %matrix.pinned, i32 %214
  39.load196 = load i32 from ptr %39195
  45198 = add i32 %244, i32 %i.phi170218
  215 = shl i32 %45198, i32 2
  47200 = ptradd ptr %matrix.pinned, i32 %215
  store i32 %39.load196 to ptr %47200
  store i32 %39.load196 to ptr %39195
  br bb %transpose.while.cond17_unified_latch
transpose.while.cond17_unified_latch:  pred(transpose.if.exit, transpose.then)
  231 = phi [bb %transpose.if.exit, i32 %39.load196, bb %transpose.then, i32 %curr.phi178]
  43197.rec = add i32 %244, i32 %6166
  br bb %transpose.while.cond17
while.body115_exclu_exit:  pred(while.body115)
  br bb %while.exit116
ans.phi.which.true:  pred(while.exit116)
  br bb %while.exit116.split
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


