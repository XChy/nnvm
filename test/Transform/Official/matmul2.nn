immutable global ptr MAX init with i32 2147483647
global ptr c init with 1000000 x i32 zeros
global ptr b init with 1000000 x i32 zeros
global ptr a init with 1000000 x i32 zeros
i32 main() {
entry:  pred()
  br bb %while.cond
while.cond:  pred(entry, if.exit)
  i.phi220 = phi [bb %entry, i32 0, bb %if.exit, i32 %22]
  10 = icmp slt i32 %i.phi220, i32 1000
  br i1 %10, bb %while.body, bb %while.exit
while.body:  pred(while.cond)
  13 = mul i32 %i.phi220, i32 4000
  14 = ptradd ptr @a, i32 %13
  15 = call i32 getarray(ptr %14)
  17 = icmp ne i32 %15, i32 1000
  br i1 %17, bb %then, bb %if.exit
while.exit:  pred(while.cond)
  call void _sysy_starttime(i32 23)
  28292 = icmp slt i32 0, i32 1000
  br i1 %28292, bb %while.body25, bb %while.exit26
then:  pred(while.body)
  ret i32 %15
if.exit:  pred(while.body)
  22 = add i32 %i.phi220, i32 1
  br bb %while.cond
while.cond24:  pred(while.exit32)
  i.phi219 = phi [bb %while.exit32, i32 %52]
  28 = icmp slt i32 %i.phi219, i32 1000
  br i1 %28, bb %while.body25, bb %while.exit26
while.body25:  pred(while.cond24, while.exit)
  i.phi219293 = phi [bb %while.exit, i32 0, bb %while.cond24, i32 %i.phi219]
  34255 = icmp slt i32 0, i32 1000
  br i1 %34255, bb %while.body31, bb %while.exit32
while.exit26:  pred(while.cond24, while.exit)
  57288 = icmp slt i32 0, i32 1000
  br i1 %57288, bb %while.body54, bb %while.exit55
while.cond30:  pred(while.body31)
  j.phi230 = phi [bb %while.body31, i32 %50]
  34 = icmp slt i32 %j.phi230, i32 1000
  br i1 %34, bb %while.body31, bb %while.exit32
while.body31:  pred(while.cond30, while.body25)
  j.phi230256 = phi [bb %while.body25, i32 0, bb %while.cond30, i32 %j.phi230]
  37 = mul i32 %i.phi219293, i32 4000
  38 = ptradd ptr @b, i32 %37
  244 = shl i32 %j.phi230256, i32 2
  41 = ptradd ptr %38, i32 %244
  43 = mul i32 %j.phi230256, i32 4000
  44 = ptradd ptr @a, i32 %43
  245 = shl i32 %i.phi219293, i32 2
  47 = ptradd ptr %44, i32 %245
  47.load = load i32 from ptr %47
  store i32 %47.load to ptr %41
  50 = add i32 %j.phi230256, i32 1
  br bb %while.cond30
while.exit32:  pred(while.cond30, while.body25)
  52 = add i32 %i.phi219293, i32 1
  br bb %while.cond24
while.cond53:  pred(while.exit61)
  temp.phi240 = phi [bb %while.exit61, i32 %temp.phi240263]
  k.phi234 = phi [bb %while.exit61, i32 %k.phi234264]
  i.phi218 = phi [bb %while.exit61, i32 %101]
  57 = icmp slt i32 %i.phi218, i32 1000
  br i1 %57, bb %while.body54, bb %while.exit55
while.body54:  pred(while.cond53, while.exit26)
  i.phi218291 = phi [bb %while.exit26, i32 0, bb %while.cond53, i32 %i.phi218]
  k.phi234290 = phi [bb %while.exit26, i32 0, bb %while.cond53, i32 %k.phi234]
  temp.phi240289 = phi [bb %while.exit26, i32 0, bb %while.cond53, i32 %temp.phi240]
  63262 = icmp slt i32 0, i32 1000
  br i1 %63262, bb %while.body60, bb %while.exit61
while.exit55:  pred(while.cond53, while.exit26)
  106286 = icmp slt i32 0, i32 1000
  br i1 %106286, bb %while.body103, bb %while.exit104
while.cond59:  pred(while.exit68)
  temp.phi239 = phi [bb %while.exit68, i32 %258]
  k.phi233 = phi [bb %while.exit68, i32 %259]
  j.phi228 = phi [bb %while.exit68, i32 %99]
  63 = icmp slt i32 %j.phi228, i32 1000
  br i1 %63, bb %while.body60, bb %while.exit61
while.body60:  pred(while.cond59, while.body54)
  j.phi228267 = phi [bb %while.body54, i32 0, bb %while.cond59, i32 %j.phi228]
  k.phi233266 = phi [bb %while.body54, i32 %k.phi234290, bb %while.cond59, i32 %k.phi233]
  temp.phi239265 = phi [bb %while.body54, i32 %temp.phi240289, bb %while.cond59, i32 %temp.phi239]
  70257 = icmp slt i32 0, i32 1000
  br i1 %70257, bb %while.body67, bb %while.exit68
while.exit61:  pred(while.cond59, while.body54)
  k.phi234264 = phi [bb %while.cond59, i32 %k.phi233, bb %while.body54, i32 %k.phi234290]
  temp.phi240263 = phi [bb %while.cond59, i32 %temp.phi239, bb %while.body54, i32 %temp.phi240289]
  101 = add i32 %i.phi218291, i32 1
  br bb %while.cond53
while.cond66:  pred(while.body67)
  temp.phi = phi [bb %while.body67, i32 %88]
  k.phi = phi [bb %while.body67, i32 %90]
  70 = icmp slt i32 %k.phi, i32 1000
  br i1 %70, bb %while.body67, bb %while.exit68
while.body67:  pred(while.cond66, while.body60)
  k.phi261 = phi [bb %while.body60, i32 0, bb %while.cond66, i32 %k.phi]
  temp.phi260 = phi [bb %while.body60, i32 0, bb %while.cond66, i32 %temp.phi]
  74 = mul i32 %i.phi218291, i32 4000
  75 = ptradd ptr @a, i32 %74
  246 = shl i32 %k.phi261, i32 2
  78 = ptradd ptr %75, i32 %246
  78.load = load i32 from ptr %78
  81 = mul i32 %k.phi261, i32 4000
  82 = ptradd ptr @b, i32 %81
  247 = shl i32 %j.phi228267, i32 2
  85 = ptradd ptr %82, i32 %247
  85.load = load i32 from ptr %85
  87 = mul i32 %78.load, i32 %85.load
  88 = add i32 %temp.phi260, i32 %87
  90 = add i32 %k.phi261, i32 1
  br bb %while.cond66
while.exit68:  pred(while.cond66, while.body60)
  259 = phi [bb %while.cond66, i32 %k.phi, bb %while.body60, i32 0]
  258 = phi [bb %while.cond66, i32 %temp.phi, bb %while.body60, i32 0]
  92 = mul i32 %i.phi218291, i32 4000
  93 = ptradd ptr @c, i32 %92
  248 = shl i32 %j.phi228267, i32 2
  96 = ptradd ptr %93, i32 %248
  store i32 %258 to ptr %96
  99 = add i32 %j.phi228267, i32 1
  br bb %while.cond59
while.cond102:  pred(while.exit138)
  i.phi217 = phi [bb %while.exit138, i32 %152]
  106 = icmp slt i32 %i.phi217, i32 1000
  br i1 %106, bb %while.body103, bb %while.exit104
while.body103:  pred(while.cond102, while.exit55)
  i.phi217287 = phi [bb %while.exit55, i32 0, bb %while.cond102, i32 %i.phi217]
  114270 = icmp slt i32 0, i32 1000
  br i1 %114270, bb %while.body111, bb %while.exit112
while.exit104:  pred(while.cond102, while.exit55)
  157284 = icmp slt i32 0, i32 1000
  br i1 %157284, bb %while.body154, bb %while.exit155
while.cond110:  pred(if.exit126)
  temp108.phi242 = phi [bb %if.exit126, i32 %temp108.phi]
  j.phi227 = phi [bb %if.exit126, i32 %135]
  114 = icmp slt i32 %j.phi227, i32 1000
  br i1 %114, bb %while.body111, bb %while.exit112
while.body111:  pred(while.cond110, while.body103)
  j.phi227273 = phi [bb %while.body103, i32 0, bb %while.cond110, i32 %j.phi227]
  temp108.phi242272 = phi [bb %while.body103, i32 2147483647, bb %while.cond110, i32 %temp108.phi242]
  117 = mul i32 %i.phi217287, i32 4000
  118 = ptradd ptr @c, i32 %117
  249 = shl i32 %j.phi227273, i32 2
  121 = ptradd ptr %118, i32 %249
  121.load = load i32 from ptr %121
  124 = icmp slt i32 %121.load, i32 %temp108.phi242272
  br i1 %124, bb %then125, bb %if.exit126
while.exit112:  pred(while.cond110, while.body103)
  271 = phi [bb %while.cond110, i32 %temp108.phi242, bb %while.body103, i32 2147483647]
  140268 = icmp slt i32 0, i32 1000
  br i1 %140268, bb %while.body137, bb %while.exit138
then125:  pred(while.body111)
  br bb %if.exit126
if.exit126:  pred(while.body111, then125)
  temp108.phi = phi [bb %while.body111, i32 %temp108.phi242272, bb %then125, i32 %121.load]
  135 = add i32 %j.phi227273, i32 1
  br bb %while.cond110
while.cond136:  pred(while.body137)
  j.phi225 = phi [bb %while.body137, i32 %150]
  140 = icmp slt i32 %j.phi225, i32 1000
  br i1 %140, bb %while.body137, bb %while.exit138
while.body137:  pred(while.cond136, while.exit112)
  j.phi225269 = phi [bb %while.exit112, i32 0, bb %while.cond136, i32 %j.phi225]
  143 = mul i32 %i.phi217287, i32 4000
  144 = ptradd ptr @c, i32 %143
  251 = shl i32 %j.phi225269, i32 2
  147 = ptradd ptr %144, i32 %251
  store i32 %271 to ptr %147
  150 = add i32 %j.phi225269, i32 1
  br bb %while.cond136
while.exit138:  pred(while.cond136, while.exit112)
  152 = add i32 %i.phi217287, i32 1
  br bb %while.cond102
while.cond153:  pred(while.exit163)
  i.phi216 = phi [bb %while.exit163, i32 %184]
  157 = icmp slt i32 %i.phi216, i32 1000
  br i1 %157, bb %while.body154, bb %while.exit155
while.body154:  pred(while.cond153, while.exit104)
  i.phi216285 = phi [bb %while.exit104, i32 0, bb %while.cond153, i32 %i.phi216]
  165274 = icmp slt i32 0, i32 1000
  br i1 %165274, bb %while.body162, bb %while.exit163
while.exit155:  pred(while.cond153, while.exit104)
  189280 = icmp slt i32 0, i32 1000
  br i1 %189280, bb %while.body186, bb %while.exit187
while.cond161:  pred(while.body162)
  j.phi223 = phi [bb %while.body162, i32 %182]
  165 = icmp slt i32 %j.phi223, i32 1000
  br i1 %165, bb %while.body162, bb %while.exit163
while.body162:  pred(while.cond161, while.body154)
  j.phi223275 = phi [bb %while.body154, i32 0, bb %while.cond161, i32 %j.phi223]
  168 = mul i32 %i.phi216285, i32 4000
  169 = ptradd ptr @c, i32 %168
  252 = shl i32 %j.phi223275, i32 2
  172 = ptradd ptr %169, i32 %252
  174 = mul i32 %j.phi223275, i32 4000
  175 = ptradd ptr @c, i32 %174
  253 = shl i32 %i.phi216285, i32 2
  178 = ptradd ptr %175, i32 %253
  178.load = load i32 from ptr %178
  180 = sub i32 0, i32 %178.load
  store i32 %180 to ptr %172
  182 = add i32 %j.phi223275, i32 1
  br bb %while.cond161
while.exit163:  pred(while.cond161, while.body154)
  184 = add i32 %i.phi216285, i32 1
  br bb %while.cond153
while.cond185:  pred(while.exit195)
  sum.phi237 = phi [bb %while.exit195, i32 %sum.phi237277]
  i.phi = phi [bb %while.exit195, i32 %211]
  189 = icmp slt i32 %i.phi, i32 1000
  br i1 %189, bb %while.body186, bb %while.exit187
while.body186:  pred(while.cond185, while.exit155)
  i.phi283 = phi [bb %while.exit155, i32 0, bb %while.cond185, i32 %i.phi]
  sum.phi237282 = phi [bb %while.exit155, i32 0, bb %while.cond185, i32 %sum.phi237]
  197276 = icmp slt i32 0, i32 1000
  br i1 %197276, bb %while.body194, bb %while.exit195
while.exit187:  pred(while.cond185, while.exit155)
  281 = phi [bb %while.cond185, i32 %sum.phi237, bb %while.exit155, i32 0]
  call void _sysy_stoptime(i32 92)
  call void putint(i32 %281)
  ret i32 0
while.cond193:  pred(while.body194)
  sum.phi = phi [bb %while.body194, i32 %207]
  j.phi = phi [bb %while.body194, i32 %209]
  197 = icmp slt i32 %j.phi, i32 1000
  br i1 %197, bb %while.body194, bb %while.exit195
while.body194:  pred(while.cond193, while.body186)
  j.phi279 = phi [bb %while.body186, i32 0, bb %while.cond193, i32 %j.phi]
  sum.phi278 = phi [bb %while.body186, i32 %sum.phi237282, bb %while.cond193, i32 %sum.phi]
  201 = mul i32 %i.phi283, i32 4000
  202 = ptradd ptr @c, i32 %201
  254 = shl i32 %j.phi279, i32 2
  205 = ptradd ptr %202, i32 %254
  205.load = load i32 from ptr %205
  207 = add i32 %sum.phi278, i32 %205.load
  209 = add i32 %j.phi279, i32 1
  br bb %while.cond193
while.exit195:  pred(while.cond193, while.body186)
  sum.phi237277 = phi [bb %while.cond193, i32 %sum.phi, bb %while.body186, i32 %sum.phi237282]
  211 = add i32 %i.phi283, i32 1
  br bb %while.cond185
}

external void putfarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putarray(i32 , ptr )

external void _sysy_stoptime(i32 )

external void putch(i32 )

external void putfloat(float )

external i32 getfarray(ptr )

external i32 getarray(ptr )

external void putint(i32 )

external float getfloat()

external i32 getch()

external i32 getint()


