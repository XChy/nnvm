global ptr COUNT init with i32 500000
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getfarray(ptr )

i32 main() {
entry27:  pred()
  %y = stack 16384
  %x = stack 16384
  31 = call i32 getint()
  call void _sysy_starttime(i32 22)
  COUNT.load158 = load i32 from ptr @COUNT
  43159 = icmp slt i32 0, i32 %COUNT.load158
  br i1 %43159, bb %while.body39_preheader, bb %while.exit40
while.body39_preheader:  pred(entry27)
  9132151 = icmp slt i32 0, i32 %31
  br bb %while.body39
while.body39:  pred(loop.while.exit, while.body39_preheader)
  i28.phi165 = phi [bb %loop.while.exit, i32 %92, bb %while.body39_preheader, i32 0]
  j.phi113164 = phi [bb %loop.while.exit, i32 %j.phi113156, bb %while.body39_preheader, i32 0]
  total.phi163 = phi [bb %loop.while.exit, float %89, bb %while.body39_preheader, float 0.000000]
  a.phi116162 = phi [bb %loop.while.exit, float %a.phi, bb %while.body39_preheader, float 0.000000]
  b.phi118161 = phi [bb %loop.while.exit, float %b.phi, bb %while.body39_preheader, float 1.000000]
  46 = srem i32 %i28.phi165, i32 10
  47 = icmp ne i32 %46, i32 0
  br i1 %47, bb %then, bb %else
while.exit40:  pred(loop.while.exit, entry27)
  160 = phi [bb %loop.while.exit, float %89, bb %entry27, float 0.000000]
  call void _sysy_stoptime(i32 39)
  95 = fsub float %160, float 11442437121638400.000000
  96 = fcmp ole float %95, float 0.000001
  br i1 %96, bb %select.then, bb %select.else
then:  pred(while.body39)
  br bb %if.exit
if.exit:  pred(then, else)
  b.phi = phi [bb %else, float %58, bb %then, float 1.000000]
  a.phi = phi [bb %else, float %55, bb %then, float 0.000000]
  64155 = icmp slt i32 %j.phi113164, i32 %31
  br i1 %64155, bb %while.body60_preheader, bb %while.exit61
else:  pred(while.body39)
  55 = fadd float %a.phi116162, float 0.100000
  58 = fadd float %b.phi118161, float 0.200000
  br bb %if.exit
while.body60_preheader:  pred(if.exit)
  br bb %while.body60
while.body60:  pred(while.body60, while.body60_preheader)
  j.phi157 = phi [bb %while.body60, i32 %84, bb %while.body60_preheader, i32 %j.phi113164]
  149 = shl i32 %j.phi157, i32 2
  68 = ptradd ptr %x, i32 %149
  72 = si2f i32 %j.phi157 to float
  73 = fadd float %a.phi, float %72
  store float %73 to ptr %68
  76 = ptradd ptr %y, i32 %149
  81 = fadd float %b.phi, float %72
  store float %81 to ptr %76
  84 = add i32 %j.phi157, i32 1
  64 = icmp slt i32 %84, i32 %31
  br i1 %64, bb %while.body60, bb %while.exit61
while.exit61:  pred(while.body60, if.exit)
  j.phi113156 = phi [bb %while.body60, i32 %84, bb %if.exit, i32 %j.phi113164]
  br i1 %9132151, bb %loop.while.body_preheader, bb %loop.while.exit
select.then:  pred(while.exit40)
  103 = fneg float 0.000001
  104 = fcmp oge float %95, float %103
  br bb %select.exit
select.else:  pred(while.exit40)
  br bb %select.exit
select.exit:  pred(select.then, select.else)
  select.phi = phi [bb %select.else, i1 0, bb %select.then, i1 %104]
  br i1 %select.phi, bb %then106, bb %else108
then106:  pred(select.exit)
  call void putint(i32 10)
  ret i32 0
else108:  pred(select.exit)
  call void putint(i32 1)
  ret i32 1
loop.while.body_preheader:  pred(while.exit61)
  br bb %loop.while.body
loop.while.body:  pred(loop.while.body, loop.while.body_preheader)
  i.phi131154 = phi [bb %loop.while.body, i32 %25142, bb %loop.while.body_preheader, i32 0]
  accumulator.phi130153 = phi [bb %loop.while.body, float %22141, bb %loop.while.body_preheader, float 0.000000]
  150 = shl i32 %i.phi131154, i32 2
  15136 = ptradd ptr %x, i32 %150
  15.load137 = load float from ptr %15136
  19138 = ptradd ptr %y, i32 %150
  19.load139 = load float from ptr %19138
  21140 = fmul float %15.load137, float %19.load139
  22141 = fadd float %accumulator.phi130153, float %21140
  25142 = add i32 %i.phi131154, i32 1
  9132 = icmp slt i32 %25142, i32 %31
  br i1 %9132, bb %loop.while.body, bb %loop.while.exit
loop.while.exit:  pred(loop.while.body, while.exit61)
  152 = phi [bb %loop.while.body, float %22141, bb %while.exit61, float 0.000000]
  89 = fadd float %total.phi163, float %152
  92 = add i32 %i28.phi165, i32 1
  43 = icmp slt i32 %92, i32 %COUNT.load158
  br i1 %43, bb %while.body39, bb %while.exit40
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external i32 getarray(ptr )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


