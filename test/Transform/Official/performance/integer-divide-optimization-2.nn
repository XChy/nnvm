global ptr size init with i32 1000
global ptr multi init with i32 2
global ptr loopCount init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry6000:  pred()
  loopCount.pinned = pin ptr @loopCount
  6004 = call i32 getint()
  store i32 %6004 to ptr %loopCount.pinned
  call void _sysy_starttime(i32 1016)
  loopCount.load601013059 = load i32 from ptr %loopCount.pinned
  601113060 = icmp slt i32 0, i32 %loopCount.load601013059
  br i1 %601113060, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry6000)
  br bb %while.body
while.body:  pred(while.body6016_exclu_exit, while.body_preheader)
  sum.phi13063 = phi [bb %while.body6016_exclu_exit, i32 %9039, bb %while.body_preheader, i32 0]
  i.phi13062 = phi [bb %while.body6016_exclu_exit, i32 %9042, bb %while.body_preheader, i32 0]
  multi.load = load i32 from ptr @multi
  6025 = mul i32 %i.phi13062, i32 %multi.load
  10039057 = sdiv i32 %6025, i32 2
  13054 = mul i32 %10039057, i32 1000
  size.load = load i32 from ptr @size
  9025 = sdiv i32 %13054, i32 %size.load
  br bb %while.body6016
while.exit:  pred(entry6000, while.body_exclu_exit)
  13061 = phi [bb %while.body_exclu_exit, i32 %9039, bb %entry6000, i32 0]
  call void _sysy_stoptime(i32 1031)
  call void putint(i32 %13061)
  call void putch(i32 10)
  ret i32 0
while.body6016:  pred(while.body6016, while.body)
  tmp.phi905113058 = phi [bb %while.body6016, i32 %9026, bb %while.body, i32 0]
  j.phi13057 = phi [bb %while.body6016, i32 %9029, bb %while.body, i32 0]
  9026 = add i32 %tmp.phi905113058, i32 %9025
  9029 = add i32 %j.phi13057, i32 1
  6019 = icmp slt i32 %9029, i32 300
  br i1 %6019, bb %while.body6016, bb %while.body6016_exclu_exit
while.body6016_exclu_exit:  pred(while.body6016)
  9032 = sdiv i32 %9026, i32 300
  9036 = add i32 %sum.phi13063, i32 %9032
  9039 = srem i32 %9036, i32 2147385347
  9042 = add i32 %i.phi13062, i32 1
  6011 = icmp slt i32 %9042, i32 %loopCount.load601013059
  br i1 %6011, bb %while.body, bb %while.body_exclu_exit
while.body_exclu_exit:  pred(while.body6016_exclu_exit)
  br bb %while.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


