global ptr size init with i32 1000
global ptr multi init with i32 2
global ptr loopCount init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry6000:  pred()
  6004 = call i32 getint()
  store i32 %6004 to ptr @loopCount
  call void _sysy_starttime(i32 1016)
  loopCount.load601013057 = load i32 from ptr @loopCount
  601113058 = icmp slt i32 0, i32 %loopCount.load601013057
  br i1 %601113058, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry6000)
  br bb %while.body
while.body:  pred(while.exit6017, while.body_preheader)
  sum.phi13061 = phi [bb %while.exit6017, i32 %9039, bb %while.body_preheader, i32 0]
  i.phi13060 = phi [bb %while.exit6017, i32 %9042, bb %while.body_preheader, i32 0]
  multi.load = load i32 from ptr @multi
  6025 = mul i32 %i.phi13060, i32 %multi.load
  10039055 = sdiv i32 %6025, i32 2
  13052 = mul i32 %10039055, i32 1000
  size.load = load i32 from ptr @size
  9025 = sdiv i32 %13052, i32 %size.load
  br bb %while.body6016
while.exit:  pred(while.exit6017, entry6000)
  13059 = phi [bb %while.exit6017, i32 %9039, bb %entry6000, i32 0]
  call void _sysy_stoptime(i32 1031)
  call void putint(i32 %13059)
  call void putch(i32 10)
  ret i32 0
while.body6016:  pred(while.body6016, while.body)
  tmp.phi905013056 = phi [bb %while.body6016, i32 %9026, bb %while.body, i32 0]
  j.phi13055 = phi [bb %while.body6016, i32 %9029, bb %while.body, i32 0]
  9026 = add i32 %tmp.phi905013056, i32 %9025
  9029 = add i32 %j.phi13055, i32 1
  6019 = icmp slt i32 %9029, i32 300
  br i1 %6019, bb %while.body6016, bb %while.exit6017
while.exit6017:  pred(while.body6016)
  9032 = sdiv i32 %9026, i32 300
  9036 = add i32 %sum.phi13061, i32 %9032
  9039 = srem i32 %9036, i32 2147385347
  9042 = add i32 %i.phi13060, i32 1
  6011 = icmp slt i32 %9042, i32 %loopCount.load601013057
  br i1 %6011, bb %while.body, bb %while.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


