global ptr global init with i32 0
global ptr loopCount init with i32 0
external i32 getint()

external i32 getch()

external void putint(i32 )

external i32 getarray(ptr )

external i32 getfarray(ptr )

i32 main() {
entry100004:  pred()
  100008 = call i32 getint()
  store i32 %100008 to ptr @loopCount
  call void _sysy_starttime(i32 100016)
  loopCount.load100014100065 = load i32 from ptr @loopCount
  100015100066 = icmp slt i32 0, i32 %loopCount.load100014100065
  br i1 %100015100066, bb %while.body_preheader, bb %while.exit
while.body_preheader:  pred(entry100004)
  br bb %while.body
while.body:  pred(while.body, while.body_preheader)
  i.phi100069 = phi [bb %while.body, i32 %100046, bb %while.body_preheader, i32 0]
  sum.phi100068 = phi [bb %while.body, i32 %100043, bb %while.body_preheader, i32 0]
  store i32 %i.phi100069 to ptr @global
  100608 = mul i32 %i.phi100069, i32 60
  100036 = sdiv i32 %100608, i32 60
  100040 = add i32 %sum.phi100068, i32 %100036
  100043 = srem i32 %100040, i32 134209537
  100046 = add i32 %i.phi100069, i32 1
  100015 = icmp slt i32 %100046, i32 %loopCount.load100014100065
  br i1 %100015, bb %while.body, bb %while.body_exclu_exit
while.exit:  pred(entry100004, while.body_exclu_exit)
  100067 = phi [bb %while.body_exclu_exit, i32 %100043, bb %entry100004, i32 0]
  call void _sysy_stoptime(i32 100032)
  call void putint(i32 %100067)
  call void putch(i32 10)
  ret i32 0
while.body_exclu_exit:  pred(while.body)
  br bb %while.exit
}

external float getfloat()

external void putfloat(float )

external void putch(i32 )

external void _sysy_stoptime(i32 )

external void putarray(i32 , ptr )

external void _sysy_starttime(i32 )

external void putfarray(i32 , ptr )


