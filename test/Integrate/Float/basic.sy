float add(float a, float b) {
    return a * b + a;
}

int main() {
    //putfloat(add(1.1, 1.2));
    //putch(10);
    //if(1.1)
    //    putint(1);
    float a = 1.0;
    if(a > 0)
        putint(2);
    putfloat(1.1 + 2 * 3 / 2.2);
    return 0;
}
