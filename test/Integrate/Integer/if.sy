// INPUT:1 4 2 3
// EXPECTED:43

int mul2(int a) {
    return a + a;
}

int main() {
    int a = getint();
    int b = getint();
    if (a < b) 
        putint(mul2(getint()));
    if (a >= b)
        putint(666);
    putint(getint());
    return 0;
}
